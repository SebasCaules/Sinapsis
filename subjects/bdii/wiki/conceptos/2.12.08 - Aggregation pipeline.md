---
tipo: concepto
tema: Aggregation pipeline — el GROUP BY, el JOIN y el ORDER BY de MongoDB como una cadena de etapas ($match, $group, $sort, $project, $unwind, $lookup) que se escribe en el orden en que se ejecuta
resumen: "Cadena de etapas ($match, $group, $sort, $lookup, $unwind) que hace de WHERE, GROUP BY, JOIN y ORDER BY en MongoDB, escrita en orden de ejecución. $group exige _id; el resto, acumuladores; $lookup devuelve arreglos, y $unwind, $lookup y otro $unwind es el join 'a la SQL', que actúa como INNER JOIN."
unidad: 2
clases: [12, 13, 14]
orden: 8
aliases:
  - Aggregation pipeline
  - Pipeline de agregación
  - Agregaciones en MongoDB
  - Función de agregación en MongoDB
  - Aggregated Queries
  - aggregate
  - aggregate()
  - $group
  - $match
  - $sort
  - $project
  - $unwind
  - $lookup
  - $limit
  - $out
  - Acumuladores
  - Ensamble de colecciones
  - Left outer join en MongoDB
  - Etapas del pipeline
fuentes:
  - "raw/Unidad-02/Teorica/BD2_Clase 12 - Introduccion a NoSQL.pdf"
  - "raw/Unidad-02/Teorica/BD2_Clase 13 - NoSQL-EmbebidosVSNormalizado(1).pdf"
  - "raw/Unidad-02/Teorica/BD2_Clase 14 - MongoDB Features.pdf"
  - "raw/Unidad-02/Teorica/Consigna MONGO DB.pdf"
  - "raw/Unidad-02/Teorica/Consigna MONGO DB (solucion).pdf"
  - "raw/Unidad-02/Practica/ITBA TP 9 - MongoDB Parte I.pdf"
estado: procesado
---

# Aggregation pipeline

> [!note] Concepto **nuevo** en el vault: aparece por primera vez en el **slide 47** de la [[Clase 12 - Introduccion a NoSQL|Clase 12]]
> El slide 46 da los datos *(tres documentos de `mycol`)* y el 47 el primer `aggregate` de la
> cursada, bajo el título *"Función de Agregación"* —el nombre de SQL, no el de MongoDB—. Los
> slides 48–49 *("Ensamble de Colecciones")* traen `$lookup`. **Es uno de los dos conceptos de la U2
> que atraviesan los tres decks del 14/09** *(el otro es
> [[2.12.06 - Modelo de documentos — JSON, BSON y ObjectId|Modelo de documentos]])*: la
> [[Clase 13 - NoSQL-EmbebidosVSNormalizado|Clase 13]]
> lista cinco etapas y desarrolla `$lookup` *(slides 21–25)*, y la
> [[Clase 14 - MongoDB Features|Clase 14]] trae el diagrama oficial del pipeline, `$sort` y `$project`
> *(slides 31–34)*. Verificado contra los PNG: los diez slides son imagen o captura, y la capa de
> texto de los PDF los pierde.
>
> Lo que **ningún deck** trae —`$unwind` en uso, `$limit`, `$multiply` dentro de `$sum`, `$push`,
> `$out`— lo trae la **consigna de ecommerce y su solución oficial** *(handout sin número, transcripto
> en [[Clase 14 - MongoDB Features]] § *Material complementario*)* y el **TP9**, cuyos ejercicios 10 y
> 11 piden `$group` sin haberlo enseñado *([[Práctica 2026-09-15]])*. Esta página junta las tres
> fuentes. La numeración `2.12.08` sigue la convención del vault: la clase de **primera aparición**.

El pipeline es la respuesta de MongoDB a una pregunta que el modelo de documentos deja abierta: si no
hay `JOIN` ni `GROUP BY` en `find()`, ¿cómo se agrupa, se cuenta y se cruza? La respuesta no es una
cláusula sino **una cadena de etapas**, y ése es el cambio de forma que hay que entender.

## Qué es

> [!quote] Clase 12, slide 47 — el primer pipeline, textual *(de la captura)*
> ```js
> > db.mycol.aggregate([{$group : {_id : "$by_user", num_tutorial : {$sum : 1}}}])
> {
>    "result" : [
>       {
>          "_id" : "tutorials point",
>          "num_tutorial" : 2
>       },
>       {
>          "_id" : "Neo4j",
>          "num_tutorial" : 1
>       }
>    ],
>    "ok" : 1
> }
> >
> ```
> *"En SQL: **select by_user, count(\*) from mycol group by by_user**."*

> [!quote] Seven Databases 2ª ed., cap. 4 § *Aggregated Queries*, impresa 115 — la definición que los decks no dan, textual
> *"The `aggregate()` method is more complex but also much more powerful. It enables you to specify a
> pipeline-style logic consisting of stages such as: `$match` filters that return specific sets of
> documents; `$group` functions that group based on some attribute; a `$sort()` logic that orders the
> documents by a sort key; and many others."*
>
> *"You can chain together as many stages as you'd like, mixing and matching at will. Think of
> `aggregate()` as a combination of `WHERE`, `GROUP BY`, and `ORDER BY` clauses in SQL. The analogy
> isn't perfect, but the aggregation API does a lot of the same things."*

**La gramática**, que el diagrama del slide 31 de la Clase 14 muestra y ningún slide enuncia:

| Pieza | Qué es | Regla |
| --- | --- | --- |
| `db.coleccion.aggregate([ … ])` | el método; recibe **un arreglo de etapas** | el arreglo puede tener una etapa o veinte |
| `{ $etapa: { … } }` | **una etapa**: un documento con **una sola clave**, el nombre de la etapa con `$` | dos claves en el mismo documento es error |
| el orden del arreglo | el **orden de ejecución**: los documentos **fluyen** de una etapa a la siguiente | la salida de una es la entrada de la otra, como una tubería de Unix |
| `"$campo"` *(con `$` y entre comillas)* | **referencia al valor** de un campo del documento que pasa por la etapa | `campo` sin `$` es un **nombre nuevo** que se crea en la salida |
| la salida | un **cursor** de documentos, igual que `find()` | *(desde 2.6; el slide 47 muestra el formato anterior → § 7)* |

> [!important] 🎯 El cambio de forma respecto de SQL: **el orden lo escribe uno**
> En SQL el orden lógico de evaluación es **fijo** —`FROM → WHERE → GROUP BY → HAVING → SELECT →
> ORDER BY → LIMIT`— y la sentencia se escribe en otro orden
> *([[1.05.01 - SQL — consultas|SQL — consultas]] § 1)*. En el pipeline **no hay orden fijo**: las
> etapas se ejecutan en el orden en que están escritas, **se pueden repetir** *(dos `$match`, dos
> `$group`, dos `$lookup` en el mismo pipeline: la solución del ecommerce lo hace)* y se pueden
> intercalar libremente. Un `$match` después de un `$group` es el `HAVING`; antes, el `WHERE`. Es la
> misma etapa: lo que cambia es la posición.

## 1 · Las etapas — las que aparecen en la cursada

> [!quote] Clase 13, slide 21 — *"Agregaciones"*, textual
> *Lookup · Sort · Match · Unwind · Project*

Cinco nombres sin `$`, sin descripción y sin `Group` —que es la etapa del slide 47 de la Clase 12 y la
única que el TP9 exige—. La tabla completa de lo que la cursada usa, con dónde aparece cada una:

| Etapa | Qué hace | Equivalente SQL | Dónde aparece | Desde |
| --- | --- | --- | --- | :---: |
| **`$match`** | filtra documentos con la misma sintaxis de `find()` | `WHERE` *(o `HAVING`, si va después de `$group`)* | C13 s21 *(nombre)* · C14 s31, s34 | 2.2 |
| **`$group`** | agrupa por `_id` y **acumula** | `GROUP BY` + funciones de agregación | C12 s47 · C14 s31, s33, s34 · handout · TP9 ej. 10–11 | 2.2 |
| **`$sort`** | ordena; `1` ascendente, `-1` descendente | `ORDER BY` | C13 s21 *(nombre)* · C14 s33, s34 · handout · TP9 | 2.2 |
| **`$project`** | elige, renombra y calcula campos; `0`/`1` como en la proyección de `find()` | `SELECT` *(lista de columnas y expresiones)* | C13 s21 *(nombre)* · C12 s51 *(dentro de una vista; el s50 solo trae la sintaxis con `[<pipeline>]`)* · C14 s34 · handout · TP9 ej. 10 | 2.2 |
| **`$lookup`** | *left outer join* con otra colección de la **misma base** | `LEFT JOIN` | C12 s48–49 · C13 s22–24 · handout | **3.2** |
| **`$unwind`** | **desarma un arreglo**: un documento de salida por elemento | no tiene *(es el inverso de embeber)* | C13 s21 *(solo el nombre)* · handout *(en los cinco pipelines)* | 2.2 |
| `$limit` · `$skip` | corta / saltea documentos | `LIMIT` · `OFFSET` | `$limit`: handout P2, P4 · `$skip`: ningún deck ni handout | 2.2 |
| `$out` | **escribe** el resultado en una colección *(la reemplaza)* | `CREATE TABLE … AS SELECT` | equivalente del `mapReduce` del handout | 2.6 |
| `$merge` | ídem, pero **fusiona** con lo que hay | `INSERT … ON DUPLICATE KEY UPDATE` sobre una tabla derivada | ningún deck ni el handout; lo introduce la **página** de la [[Clase 14 - MongoDB Features]] § (c) al traducir `mapReduce` | 4.2 |
| `$count` | cuenta lo que llega a la etapa | `SELECT COUNT(*)` sin `GROUP BY` | ningún deck | 3.4 |

*(Columna "Desde": versión de MongoDB en que la etapa existe; verificada contra la documentación
oficial, no contra un libro del vault → § Bibliografía.)*

> [!important] 🎯 Las cuatro que hay que dominar y el orden en que conviene aprenderlas
> 1. **`$group`** — sin ella no hay agregación; es lo que el TP9 pide.
> 2. **`$match`** — y su posición: antes de `$group` **reduce trabajo y puede usar índices**; después
>    de `$group` ya no hay índice que valga *(el `_id` del grupo es un valor calculado)*.
> 3. **`$lookup`** — el join, con la particularidad de que devuelve **arreglos**, no filas *(§ 3)*.
> 4. **`$unwind`** — lo que convierte esos arreglos en "filas" y lo que hace consultable un documento
>    con arreglos embebidos *(§ 4)*. Sin ella, `$lookup` es casi inútil para sumar.
>
> `$sort`, `$project` y `$limit` son las mismas ideas que en SQL, con otra grafía.

## 2 · `$group` — la etapa central

> [!quote] Clase 14, slide 33 — la traducción SQL → pipeline más limpia de los tres decks, textual
> ```sql
> SELECT tipo, Count(*)
> FROM hospitales
> GROUP BY tipo
> ORDER BY Count(*) DESC
> ```
> ```js
> db.hospitales.aggregate([
>  { $group : {_id: "$properties.TIPO", count:{$sum:1} } },
>  { $sort : { count : -1 } }
> ])
> ```
> ```
> { "_id" : "Hospital especializado", "count" : 20 }
> { "_id" : "Hospital de agudos", "count" : 13 }
> { "_id" : "Hospital de niños", "count" : 3 }
> ```

Anatomía de un `$group`:

| Parte | En el slide 33 | Regla |
| --- | --- | --- |
| **`_id`** | `"$properties.TIPO"` | **obligatoria**: es la **clave de agrupación**. `null` o una constante *(`'averagePopulation'`, slide 34)* agrupa **todo en un solo documento**. Puede ser un documento `{ a: "$a", b: "$b" }` para agrupar por varios campos |
| **acumuladores** | `count: { $sum: 1 }` | cada clave que no es `_id` es un **campo nuevo de salida** calculado con un **acumulador** sobre todos los documentos del grupo |
| notación de punto | `"$properties.TIPO"` | los campos anidados se referencian igual que en `find()` |

**Los acumuladores que aparecen en la cursada**, más los dos que faltan y hacen falta:

| Acumulador | Qué devuelve | En SQL | Dónde aparece |
| --- | --- | --- | --- |
| `{ $sum: 1 }` | cantidad de documentos del grupo | `COUNT(*)` | C12 s47 · C14 s33 · TP9 ej. 11 |
| `{ $sum: "$campo" }` | suma del campo | `SUM(campo)` | C14 s31 · handout P2, P3 |
| `{ $sum: { $multiply: ["$a", "$b"] } }` | suma de una **expresión** | `SUM(a * b)` | handout P1, P3 *(el "cantidad × precio")* |
| `{ $avg: "$campo" }` | promedio; **ignora** los documentos sin el campo o con valor no numérico | `AVG(campo)` | C14 s34 · TP9 ej. 10 |
| `$min` · `$max` | mínimo y máximo | `MIN` · `MAX` | ninguno *(pero es lo que pide "el más vendido" bien resuelto)* |
| `{ $push: "$campo" }` | **arreglo** con el valor de cada documento del grupo, repetidos incluidos | no tiene *(lo más cercano: `GROUP_CONCAT` de MySQL, que devuelve un string)* | handout P5 |
| `{ $addToSet: "$campo" }` | ídem, sin repetidos | — | ninguno |
| `$first` · `$last` | el valor del primer / último documento del grupo *(solo tiene sentido después de un `$sort`)* | — | propuesta propia del vault para la P5 del handout |
| `{ $count: {} }` | igual que `{ $sum: 1 }` | `COUNT(*)` | ninguno; **desde 5.0** |

> [!important] 🎯 En `$group` **no existe la "columna no agrupada"**: todo lo que no es `_id` pasa por un acumulador
> En MySQL, `SELECT nombre, barrio, COUNT(*) … GROUP BY barrio` compila o no según
> `ONLY_FULL_GROUP_BY` *(el recuadro *GROUP BY in MySQL* de Seven Databases, impresa 24, es
> exactamente sobre eso)*. En `$group` la pregunta no se plantea: si se quiere conservar `nombre`,
> hay que decir **cuál** —`nombre: { $first: "$nombre" }`— o agruparlo *(`$push`)*. Es la versión
> estricta de la regla que la [[1.05.01 - SQL — consultas|Clase 05]] enseñó para `GROUP BY`.

> [!tip] Las dos caras del pipeline *(Clase 14, slide 34)*
> El slide 34 pone juntas dos consultas sobre `cities`: una con `$match → $group` que **colapsa**
> N documentos en uno *(agregación en sentido estricto)*, y otra con `$match → $sort → $project` que
> **transforma** N documentos en N, **sin agrupar**. Las dos son `aggregate`. Para la segunda,
> `find(filtro, proyección).sort(…)` da lo mismo y es más corto; el pipeline empieza a valer la pena
> cuando hace falta **encadenar** o cuando hace falta `$lookup` o `$unwind`, que `find()` no tiene.

## 3 · `$lookup` — el *left outer join*

Aparece **dos veces, con el mismo patrón**: `posts`/`comments` en la Clase 12 *(slides 48–49,
"Ensamble de Colecciones")* y `orders`/`inventory` en la Clase 13 *(slides 22–24, "Lookup: Left outer
join")*.

> [!quote] Clase 13, slide 23 — textual *(de la captura)*
> ```js
> db.orders.aggregate([
>    {
>      $lookup:
>        {
>          from: "inventory",
>          localField: "item",
>          foreignField: "sku",
>          as: "inventory_docs"
>        }
>    }
> ])
> ```

| Parámetro | Valor en el slide 23 | En SQL |
| --- | --- | --- |
| *(colección sobre la que se agrega)* | `orders` | `FROM orders` — **el lado izquierdo, el que se conserva entero** |
| `from` | `"inventory"` | `LEFT JOIN inventory` |
| `localField` | `"item"` | la mitad izquierda del `ON`: `orders.item` |
| `foreignField` | `"sku"` | la mitad derecha: `inventory.sku` |
| `as` | `"inventory_docs"` | **sin equivalente**: el nombre del **arreglo** donde van los documentos de `inventory` que emparejan |

> [!important] 🎯 `$lookup` **no devuelve filas: devuelve documentos con un arreglo adentro**
> En SQL el join produce **una fila por pareja** —una orden con tres productos sale tres veces—.
> `$lookup` produce **un documento por orden**, con `inventory_docs: [ … ]` que contiene **todas**
> las parejas; si no hay ninguna, el arreglo queda **vacío** `[]` y el documento **sale igual** —por
> eso es *left*—. O sea: **el join reconstruye, en tiempo de consulta, el modelo embebido a partir
> del normalizado**. El resultado del pipeline del slide 49 de la Clase 12 *(que el slide no muestra;
> derivado en [[Clase 12 - Introduccion a NoSQL]] § Slides 48–49)* tiene exactamente la forma del
> slide 34 de la misma clase *(comentarios dentro del post)*. Ésa es la relación entre este concepto y
> [[2.13.01 - Documentos embebidos vs. referencias|Documentos embebidos vs. referencias]]: embeber es
> **precomputar el `$lookup`** al escribir.
>
> Para obtener filas al estilo SQL hay que agregar **`$unwind: "$inventory_docs"`** después → § 4.

> [!warning] 🔴 Tres diferencias con el `LEFT JOIN` que el título del slide 23 no dice
> 1. **`null` empareja con `null` y con "ausente".** La orden 3 del slide 22 **no tiene `item`**;
>    para `$lookup` eso vale `null`, y empareja con `inventory` 5 *(`sku: null`)* **y** con 6 *(sin
>    `sku`)*. En SQL `NULL = NULL` no es verdadero y esa fila saldría con el lado derecho vacío
>    *([[1.05.01 - SQL — consultas|SQL — consultas]] § 11)*. La captura del slide 24 se corta
>    **justo antes** de mostrar este caso —el deck armó los datos para enseñarlo y el recorte se lo
>    comió—. Va a § Dudas abiertas.
> 2. **`from` tiene que ser una colección de la misma base de datos.** No hay join entre bases.
> 3. **Versiones:** `$lookup` existe desde **3.2** *(2015)*; sobre una colección `from`
>    *sharded* recién desde **5.1**. Seven Databases 2ª ed. *(2018, MongoDB 3.6)* **no lo trae**
>    *(cero ocurrencias en el cap. 4)*: la posición del libro es que Mongo *"isn't built to perform
>    joins"* *(§ *References*, impresa 106)*. Los decks son más nuevos que el libro en este punto.

> [!bug] Los nombres de colección de la Clase 12 no coinciden entre el slide 48 y el 49
> El 48 titula *"post collection"* y *"comment collection"*; el 49 consulta `db.posts` con
> `from: "comments"`. Con los nombres del 48 el pipeline devuelve **vacío sin error** —una colección
> inexistente da cero documentos—. Y `postTitle` referencia a `title`, un **string**: funciona con
> tres posts, y se rompe con dos títulos iguales. El patrón correcto referencia el `_id` *(el handout
> del ecommerce lo hace bien: `cliente_id`, `items.producto_id`)*.

**La forma con `pipeline` y `let`** *(desde 3.6, no está en la cursada)* permite condiciones de join
distintas de la igualdad y filtrar el lado derecho antes de emparejar. Se nombra porque es lo que
hace falta para un join con `<`, `>` o con dos campos, y porque *Practical MongoDB Aggregations*
*(el libro que la Clase 14 nombra en el slide 32)* lo usa. **Sin ficha en el vault.**

## 4 · `$unwind` — la etapa que une las dos mitades de la Clase 13

El slide 21 la nombra y ningún slide la usa. **La solución oficial del ecommerce la usa en los cinco
pipelines**, y siempre con el mismo patrón:

> [!quote] Handout *Consigna MONGO DB (solucion)*, pregunta 1 — total gastado por cliente, textual
> ```js
> db.ordenes.aggregate([
>   { $unwind: "$items" },
>   { $lookup: { from: "productos", localField: "items.producto_id", foreignField: "_id", as: "producto" } },
>   { $unwind: "$producto" },
>   { $group: { _id: "$cliente_id", total: { $sum: { $multiply: ["$items.cantidad", "$producto.precio"] } } } },
>   { $lookup: { from: "clientes", localField: "_id", foreignField: "_id", as: "cliente" } },
>   { $unwind: "$cliente" },
>   { $project: { _id: 0, nombre: "$cliente.nombre", email: "$cliente.email", totalGastado: "$total" } },
>   { $sort: { totalGastado: -1 } }
> ]);
> ```

Etapa por etapa, con la cantidad de documentos que salen de cada una para los datos del handout
*(3 órdenes, 5 ítems en total)*:

| # | Etapa | Qué hace con el documento | Salen |
| :---: | --- | --- | :---: |
| 1 | `$unwind: "$items"` | la orden con 2 ítems se vuelve **2 documentos**, cada uno con `items` como **subdocumento** en vez de arreglo | 5 |
| 2 | `$lookup` productos | agrega `producto: [ {…} ]` — **siempre un arreglo**, aunque empareje uno | 5 |
| 3 | `$unwind: "$producto"` | desarma ese arreglo de un elemento → `producto.precio` es un **escalar** usable | 5 |
| 4 | `$group` por `cliente_id` | suma `cantidad × precio` | 3 |
| 5–6 | `$lookup` clientes + `$unwind` | trae nombre y email **después** de agrupar: cruza 3 documentos, no 5 | 3 |
| 7–8 | `$project` + `$sort` | forma final, de mayor a menor | 3 |

**Resultado:** Juan Pérez 12 000 · Carlos Díaz 9 700 · María Gómez 6 000 — coincide con la cuenta a
mano de [[Clase 14 - MongoDB Features]] § *Material complementario* (a).

> [!important] 🎯 El patrón **`$unwind` → `$lookup` → `$unwind`** es el join "a la SQL", y hay que saberlo de memoria
> - El **primer** `$unwind` desarma el arreglo **embebido** *(`items`)* para que cada elemento sea un
>   documento con su propia clave foránea.
> - El `$lookup` trae el documento referenciado, **como arreglo**.
> - El **segundo** `$unwind` desarma ese arreglo para poder usar `"$producto.precio"` en una
>   expresión. Sin él, `"$producto.precio"` es un arreglo y `$multiply` **aborta el pipeline con
>   error** *(`$multiply only supports numeric types, not array`; devuelve `null` solo si el campo
>   falta o es `null` — según documentación, no comprobado en servidor → § Dudas abiertas)*.
>
> Dicho en los términos de la Clase 13: `$lookup` **junta lo que está separado** *(referencias)* y
> `$unwind` **separa lo que está junto** *(embebido)*. Un modelo embebido se consulta con `$unwind`;
> uno normalizado, con `$lookup`; el del ecommerce —ítems embebidos en la orden, productos y clientes
> referenciados— necesita los dos.

> [!warning] `$unwind` **descarta** los documentos cuyo arreglo está vacío o no existe
> Después de un `$lookup` sin pareja el arreglo queda `[]`; el `$unwind` siguiente **elimina ese
> documento**. En la práctica, **`$lookup` + `$unwind` es un `INNER JOIN`**, no un `LEFT JOIN`. Para
> conservar el *left*, `{ $unwind: { path: "$producto", preserveNullAndEmptyArrays: true } }`
> *(desde 3.2)*. Con los datos del handout no se nota —todo ítem tiene producto—; con un cliente sin
> órdenes o una orden con un `producto_id` roto, sí. Es la misma trampa que el `WHERE` sobre la tabla
> derecha de un `LEFT JOIN` en SQL.

> [!note] En el álgebra relacional existe el inverso de embeber, y tiene nombre
> Date 8ª ed. dedica la § **7.9** a *Grouping and Ungrouping* *(impresa 203)*: `GROUP` construye un
> atributo cuyo valor es una relación anidada y `UNGROUP` lo aplana. `$push` dentro de `$group` es el
> `GROUP` de Date; `$unwind` es su `UNGROUP`. *(Solo el título está verificado en la ficha: el cap. 7
> del PDF de Date no tiene capa de texto.)*

## 5 · Traducción SQL → pipeline — la tabla completa

Lo que la Clase 05 enseñó en [[1.05.01 - SQL — consultas|SQL — consultas]], cláusula por cláusula:

| SQL *(MySQL, U1)* | Pipeline *(MongoDB, U2)* | Nota |
| --- | --- | --- |
| `FROM t` | `db.t.aggregate([…])` | la colección base es el lado izquierdo de todo |
| `WHERE cond` | `{ $match: { cond } }` **al principio** | misma sintaxis de `find()`; puede usar índices |
| `GROUP BY a, b` | `{ $group: { _id: { a: "$a", b: "$b" }, … } }` | la clave de grupo se llama `_id` **siempre** |
| `COUNT(*)` · `SUM(x)` · `AVG(x)` · `MIN` · `MAX` | `{ $sum: 1 }` · `{ $sum: "$x" }` · `{ $avg: "$x" }` · `$min` · `$max` | dentro de `$group` |
| `SUM(a * b)` | `{ $sum: { $multiply: ["$a", "$b"] } }` | la expresión va **dentro** del acumulador |
| `HAVING cond` | `{ $match: { cond } }` **después** de `$group` | la misma etapa, otra posición |
| `SELECT a, b AS c` | `{ $project: { _id: 0, a: 1, c: "$b" } }` | `0`/`1` incluyen o excluyen; `"$b"` renombra |
| `SELECT DISTINCT a` | `{ $group: { _id: "$a" } }` · o `db.t.distinct("a")` | `distinct` devuelve un arreglo, no documentos *(C14 s34)* |
| `ORDER BY x DESC, y` | `{ $sort: { x: -1, y: 1 } }` | |
| `LIMIT n OFFSET m` | `{ $skip: m }, { $limit: n }` | el orden de las dos etapas **sí** importa aquí |
| `t LEFT JOIN u ON t.k = u.k` | `{ $lookup: { from: "u", localField: "k", foreignField: "k", as: "u" } }` | devuelve arreglo; `+ $unwind` para filas |
| `t INNER JOIN u …` | `$lookup` + `$unwind` *(sin `preserveNullAndEmptyArrays`)* | el `$unwind` descarta los sin pareja |
| `SELECT … FROM t, u WHERE t.k < u.k` | `$lookup` con `let` + `pipeline` *(3.6)* | no está en la cursada |
| `UNION ALL` | `{ $unionWith: "u" }` *(4.4)* | no está en la cursada |
| `CREATE TABLE r AS SELECT …` | `{ $out: "r" }` como **última** etapa | reemplaza `r`; `$merge` fusiona |
| `CREATE VIEW v AS SELECT …` | `db.createView("v", "t", [ pipeline ])` | → [[1.06.01 - Vistas|Vistas]] § MongoDB; TP9 ej. 10 |
| `EXPLAIN SELECT …` | `db.t.explain("executionStats").aggregate([…])` | → [[1.08.01 - Plan de ejecución|Plan de ejecución]] |

> [!tip] Cómo se traduce una consulta SQL sin equivocarse: **en orden de ejecución, no de escritura**
> Se escribe el pipeline siguiendo el orden lógico de SQL —`FROM`, `WHERE`, `GROUP BY`, `HAVING`,
> `SELECT`, `ORDER BY`, `LIMIT`— y **no** el orden en que se escribe la sentencia. El resultado es que
> el `$project` *(el `SELECT`)* queda casi al final, después del `$group`, y eso desconcierta la
> primera vez. El slide 33 de la Clase 14 es el caso mínimo: `GROUP BY` → `$group`, `ORDER BY` →
> `$sort`, y el `SELECT tipo, Count(*)` **no necesita `$project`** porque `$group` ya deja
> exactamente esos dos campos.

## 6 · Todos los pipelines de la cursada, catalogados

Para estudiar: cada `aggregate` que hay en el material, con lo que enseña.

| Fuente | Colección | Etapas | Lo que enseña |
| --- | --- | --- | --- |
| C12 s47 | `mycol` | `$group` | el primer pipeline; `_id` como clave; `{ $sum: 1 }` = `COUNT(*)`; salida en formato < 2.6 |
| C12 s49 | `posts` | `$lookup` | el join, con `from`/`localField`/`foreignField`/`as` |
| C12 s51 | `survey` → vista | `$project` | un pipeline **con nombre**: la vista *(el s50 es la sintaxis genérica de `createView`, sin etapa concreta)* |
| C13 s23 | `orders` | `$lookup` | el join otra vez; los datos del s22 muestran `null`/ausente |
| C14 s31 | `orders` | `$match` → `$group` | el diagrama oficial: el flujo documento a documento; `$sum: "$amount"` |
| C14 s33 | `hospitales` | `$group` → `$sort` | traducción de `GROUP BY … ORDER BY COUNT(*) DESC` |
| C14 s34 | `cities` | `$match` → `$group` *(`_id` constante, `$avg`)* | agrupar **todo**: el `AVG` sin `GROUP BY` |
| C14 s34 | `cities` | `$match` → `$sort` → `$project` | pipeline **sin agrupar**: N entran, N salen |
| Handout P1 | `ordenes` | `$unwind` → `$lookup` → `$unwind` → `$group` → `$lookup` → `$unwind` → `$project` → `$sort` | el patrón de join; `$multiply` en `$sum`; agrupar antes del segundo `$lookup` |
| Handout P2 | `ordenes` | `$unwind` → `$group` → `$lookup` → `$unwind` → `$project` → `$sort` → `$limit` | el "top 1"; **el dataset empata** y `$limit: 1` devuelve uno al azar |
| Handout P3 | `ordenes` | `$unwind` → `$lookup` → `$unwind` → `$group` *(dos acumuladores)* → `$project` | dos agregados en un `$group`; renombrar `_id` en `$project` |
| Handout P4 | `ordenes` | P1 + `$limit: 5` | **la solución oficial no muestra el país que pide la consigna** |
| Handout P5 | `ordenes` | `$lookup` → `$unwind` → `$unwind` → `$lookup` → `$unwind` → `$group` *(`$push`)* | `$push` arma arreglos al agrupar; **aplana las órdenes** |
| Handout MapReduce, traducido | `orders` | `$group` → `$out` | el equivalente en cuatro líneas de `mapReduce` → [[2.14.03 - MapReduce\|MapReduce]] |
| TP9 ej. 10 | `bandas_resumen` *(vista)* | `$group` *(`$avg`)* → `$sort` por `_id` | promedio por género, alfabético; **sobre una vista** |
| TP9 ej. 11 | `bandas_resumen` | `$group` *(`$sum: 1`)* → `$sort` | cantidad por barrio, descendente *(el desempate alfabético por `_id` es propuesta de [[Práctica 2026-09-15]]; el enunciado no lo pide)* |

Los detalles de cada uno —datos, resultado esperado, errores de la solución oficial— están en
[[Clase 14 - MongoDB Features]] § *Material complementario* (a) y en [[Práctica 2026-09-15]] §
*Ejercicios 10–11*. No se repiten aquí.

## 7 · En el motor de la cursada — MongoDB y `mongosh`

> [!warning] Los decks mezclan **tres épocas** de MongoDB, y el pipeline es donde más se nota
> El slide 47 de la Clase 12 muestra la salida `{ "result" : [ … ], "ok" : 1 }`, que es de **antes de
> 2.6** *(2014)*; `$lookup` *(slides 48–49)* existe desde **3.2** *(2015)*; la vista del slide 50,
> desde **3.4** *(2016)*. El deck 14 se declara sobre **6.0.5** *(slide 2: "Stable release: 6.0.5 / 2023-03-13")*. Y el TP9 corre sobre la
> imagen `mongo` **sin tag** de Docker, que trae la rama actual → [[MongoDB]]. Lo que sigue dice qué
> corre tal cual y qué no.

| Como está en el material | En `mongosh` sobre MongoDB 6.0+ | Estado |
| --- | --- | :---: |
| `db.col.aggregate([ … ])` *(todos los decks, el handout, el TP9)* | **igual** | ✅ |
| salida `{ "result" : [ … ], "ok" : 1 }` *(C12 s47)* | el shell imprime **los documentos sueltos**, uno por línea, como `find()`: `aggregate` devuelve un **cursor** desde 2.6 | 🔴 captura vieja |
| `$group`, `$match`, `$sort`, `$project`, `$lookup`, `$unwind`, `$limit` | **iguales** | ✅ |
| `{ $sum: 1 }` | igual; alternativa `{ $count: {} }` **desde 5.0** | ✅ |
| `db.phones.count(filtro)` *(C14 s34)* | **`countDocuments(filtro)`**; `count()` está deprecado en `mongosh` según la documentación de compatibilidad *(el TP9, paso 27, muestra las dos formas sin señalarlo → [[Práctica 2026-09-15]] § Paso 27)* | 🟡 deprecado |
| `db.phones.distinct(campo, filtro)` *(C14 s34)* | igual | ✅ |
| `mapReduce` *(C14 s35–37, handout)* | **`$group` + `$out`** *(deprecado desde 5.0)* → [[2.14.03 - MapReduce\|MapReduce]] | 🔴 deprecado |
| `$lookup` sobre colección *sharded* | permitido desde **5.1** | ✅ *(en el TP no hay shards)* |
| empates en un "top N" *(handout P2)* | `$setWindowFields` con `$rank` **desde 5.0**, o un segundo criterio en `$sort` | ✅ |
| `db.createView(nombre, origen, [pipeline])` *(C12 s50, TP9 ej. 10)* | igual; la vista es **de solo lectura** y no admite `$out`/`$merge` en su pipeline | ✅ |
| *"Usando MongoDB (Compass)"* *(consigna del handout)* | Compass tiene un **constructor de pipelines** gráfico *(pestaña *Aggregations*)* que exporta el arreglo a `mongosh`; la consigna no dice si vale escribirlo a mano | 🟡 a preguntar |

**Límites que ningún deck menciona y que aparecen en cuanto el dataset crece** *(documentación
oficial; versión exacta a verificar contra la imagen del TP)*:

| Límite | Valor | Cómo se esquiva |
| --- | --- | --- |
| tamaño de cada documento **de salida** | 16 MB *(el límite de BSON)* | un `$push` sin control lo alcanza; agrupar más fino |
| memoria por etapa *(`$group`, `$sort`)* | 100 MB | `aggregate(pipeline, { allowDiskUse: true })` *(en 6.0 el servidor lo habilita por defecto: a verificar)* |
| índices | solo los usa `$match` **al principio** *(y `$sort` si va antes de cualquier transformación)* | poner `$match` y `$sort` lo más arriba posible; el optimizador reordena algunos casos solo |

> [!tip] `explain` funciona sobre el pipeline, y es la forma de ver si el `$match` usó el índice
> `db.bandas.explain("executionStats").aggregate([ { $match: { barrio: "BARRACAS" } }, { $group: … } ])`
> muestra `IXSCAN` o `COLLSCAN` en la primera etapa, igual que para `find()` *(Clase 13, slide 28)*.
> El plan de un pipeline es el de la [[1.08.01 - Plan de ejecución|Clase 08]] con otra sintaxis:
> lo que está después de la primera etapa que transforma documentos **no tiene índice posible**.

## Cuándo usarlo y cuándo no

| Usar `aggregate` cuando… | Usar `find()` cuando… | Cambiar el **modelo** cuando… |
| --- | --- | --- |
| hay que **agrupar** o **contar por algo** *(`$group`)* | solo se filtra, ordena, proyecta y recorta | cada consulta frecuente necesita un `$lookup`: la Clase 13 dice que eso es señal de que había que **embeber** |
| hay que **cruzar colecciones** *(`$lookup`)* | la respuesta es un subconjunto de una colección, tal cual | el `$push` de un `$group` produce arreglos que crecen sin cota: la regla del crecimiento de la Clase 13, slide 18 |
| hay que **desarmar un arreglo** para contar por elemento *(`$unwind`)* | | |
| hay que **calcular** campos *(`$multiply`, `$concat`)* o **materializar** *(`$out`)* | | |

> [!important] 🎯 La doctrina de la cursada, en una frase: **`$lookup` es el join que se hace cuando no se pudo evitar**
> La Clase 13 enseña a **diseñar** para que la consulta frecuente no necesite join; el pipeline es la
> herramienta para cuando el dato está normalizado *(porque crece, porque se comparte, porque se
> actualiza en un solo lugar)*. La respuesta correcta a *"¿cómo obtengo X?"* en un examen de
> MongoDB tiene dos partes: **cómo lo modelo** y, si quedó referenciado, **qué pipeline lo cruza**.

## Para el parcial

| Pregunta probable | Respuesta corta, con cita |
| --- | --- |
| ¿Qué es el *aggregation pipeline*? | Una cadena de **etapas** que los documentos atraviesan en orden; la salida de una es la entrada de la siguiente. *"Think of `aggregate()` as a combination of `WHERE`, `GROUP BY`, and `ORDER BY`"* *(Seven Databases, impresa 115)* |
| ¿Cómo se escribe el `GROUP BY` en MongoDB? | `{ $group: { _id: "$campo", n: { $sum: 1 } } }` — `_id` es la clave de grupo, obligatoria; las demás claves son acumuladores *(C12 s47: `select by_user, count(*) from mycol group by by_user`)* |
| ¿Qué significa el `$` delante de un nombre? | Dentro de una etapa, `"$campo"` es **el valor** del campo del documento; `campo` sin `$` es un nombre nuevo de salida *(C14 s31: `total: { $sum: "$amount" }`)* |
| ¿Dónde va el `WHERE` y dónde el `HAVING`? | Los dos son `$match`: **antes** de `$group` es `WHERE` *(y usa índices)*; **después** es `HAVING` *(C14 s31 para el primero; propuesta propia para el segundo)* |
| ¿Cómo se hace un join? | `$lookup` con `from`, `localField`, `foreignField`, `as` *(C12 s49, C13 s23)*. Es un **left outer join** que devuelve **un arreglo** dentro de cada documento, no filas |
| ¿Qué diferencia hay con el `LEFT JOIN` de SQL? | Devuelve arreglos y no filas; `null` empareja con `null` y con campo ausente; solo dentro de la misma base *(C13 s22–24)* |
| ¿Para qué sirve `$unwind`? | Para convertir un arreglo en **un documento por elemento**; después de un `$lookup`, para poder usar el documento emparejado como escalar. `$unwind` + `$lookup` + `$unwind` es el patrón de join *(handout P1)* |
| ¿`$unwind` conserva los documentos con arreglo vacío? | **No**, salvo `preserveNullAndEmptyArrays: true`. Por eso `$lookup` + `$unwind` se comporta como `INNER JOIN` |
| ¿Cómo se escribe `SELECT tipo, COUNT(*) … GROUP BY tipo ORDER BY COUNT(*) DESC`? | `[ { $group: { _id: "$tipo", count: { $sum: 1 } } }, { $sort: { count: -1 } } ]` *(C14 s33, textual)* |
| ¿Cómo se calcula "precio × cantidad" agrupado? | `{ $sum: { $multiply: ["$items.cantidad", "$producto.precio"] } }` *(handout P1, P3)* |
| ¿Qué relación hay con `mapReduce`? | El pipeline lo reemplaza: `$group` hace de `map` + `reduce`, `$out` de `out`, `$project` de `finalize`; `mapReduce` está **deprecado desde 5.0** *(handout MapReduce traducido; → [[2.14.03 - MapReduce\|MapReduce]])* |
| ¿Qué es una vista en MongoDB? | Un **pipeline con nombre**, de solo lectura, re-ejecutado en cada lectura *(C12 s50; TP9 ej. 10 → [[1.06.01 - Vistas\|Vistas]])* |
| ¿Cuándo conviene evitar el `$lookup`? | Cuando la consulta es frecuente y el dato cabe embebido: la Clase 13 dice que se embebe lo que se lee junto; el join queda para lo que crece sin cota o se comparte *(→ [[2.13.01 - Documentos embebidos vs. referencias\|Documentos embebidos vs. referencias]])* |

> [!tip] 🎯 Cómo escribir un pipeline en el examen sin equivocarse
> 1. Escribir la consulta **en SQL** primero, aunque no se entregue: fija qué se agrupa, qué se
>    filtra y qué se ordena.
> 2. Traducirla **en orden de ejecución**: `$match` → *(`$unwind` si hay arreglo)* → *(`$lookup` +
>    `$unwind` si hay referencia)* → `$group` → *(`$match` si hay `HAVING`)* → `$sort` → `$project`
>    → `$limit`.
> 3. Comprobar que **todo lo que se proyecta al final existe** después del `$group`: lo que no pasó
>    por `_id` o por un acumulador **desapareció**.
> 4. Decir en una línea cuántos documentos salen de cada etapa con los datos del enunciado —es lo
>    que descubre el empate de la P2 y la orden aplanada de la P5 del handout.

## Cuadro de bolsillo

| Pregunta | Respuesta |
| --- | --- |
| Forma | `db.col.aggregate([ { $etapa1: … }, { $etapa2: … }, … ])` — un arreglo, una etapa por documento, **en orden de ejecución** |
| `$match` | `WHERE` antes de `$group`, `HAVING` después; sintaxis de `find()` |
| `$group` | `_id` = clave de grupo *(obligatoria; `null` = todo)*; el resto, acumuladores |
| Acumuladores | `$sum: 1` *(COUNT)* · `$sum: "$x"` · `$avg` · `$min` · `$max` · `$push` · `$addToSet` · `$first` · `$last` |
| `"$x"` vs. `x` | valor del campo de entrada vs. nombre del campo de salida |
| `$sort` | `{ campo: 1 \| -1 }` |
| `$project` | `0`/`1` para incluir o excluir; `nuevo: "$viejo"` renombra; `_id: 0` para ocultarlo |
| `$lookup` | `from` · `localField` · `foreignField` · `as` → **arreglo**; *left outer*; misma base; 3.2 |
| `$unwind` | un documento por elemento del arreglo; **descarta** vacíos salvo `preserveNullAndEmptyArrays` |
| El join "a la SQL" | `$unwind` → `$lookup` → `$unwind` |
| `$limit` · `$skip` · `$out` · `$merge` · `$count` | `LIMIT` · `OFFSET` · `CREATE TABLE AS` · *upsert* masivo · `COUNT(*)` |
| Salida | un **cursor** de documentos *(desde 2.6; el `{result:[…], ok:1}` del slide 47 es anterior)* |
| Índices | solo en `$match`/`$sort` iniciales |
| Límites | 16 MB por documento de salida · 100 MB por etapa *(`allowDiskUse`)* |
| `mapReduce` | deprecado desde 5.0 → `$group` + `$out` |
| Vista | `createView(nombre, origen, [pipeline])` = pipeline con nombre |
| Primera aparición | Clase 12, slide 47 |

## Bibliografía verificada

Todo verificado contra las fichas de `raw/Material_Catedra/bibliografia/` y, para las citas
textuales, contra el PDF con `pdftotext` en las páginas indicadas.

| Fuente | Sección | Impresa · PDF | Qué aporta |
| --- | --- | --- | --- |
| ⭐⭐ [[Seven Databases in Seven Weeks — ficha\|Seven Databases]] 2ª ed. | cap. 4 › Day 2 › § **Aggregated Queries** | impresas **115–117** · PDF 128–130 | **La única sección de libro del vault sobre el pipeline**: `count`, `distinct`, `aggregate`; la definición por etapas y la analogía `WHERE`/`GROUP BY`/`ORDER BY`; los dos pipelines sobre `cities` que el slide 34 de la Clase 14 copia; la advertencia final sobre el costo *("aggregations can be quite slow…")*. **Sin `$lookup` ni `$unwind`** *(cero ocurrencias en el cap. 4)*. Su nota al pie 3 es la misma URL del slide 25 de la Clase 13 |
| ⭐ Seven Databases | cap. 4 › Day 1 › § **References** | impresas **106–107** · PDF 119–120 | *"Mongo isn't built to perform joins"*: la postura del libro *(2018)* que `$lookup` matiza |
| Seven Databases | cap. 4 › Day 2 › § **Mapreduce (and Finalize)** | impresas **119–123** · PDF 132–136 | lo que el pipeline reemplaza → [[2.14.03 - MapReduce\|MapReduce]] |
| Seven Databases | cap. 4 › Day 2 › § **Indexing: When Fast Isn't Fast Enough** | impresas **110–114** · PDF 123–127 | `explain("executionStats")`, aplicable al `$match` inicial |
| Seven Databases | cap. 2 › recuadro ***GROUP BY in MySQL*** | impresa **24** · PDF 38 | la "columna no agrupada" que `$group` no admite *(§ 2)* |
| ⭐ [[Database Systems The Complete Book — ficha\|GMUW]] | **6.4.3–6.4.7** *Grouping and Aggregation in SQL · Aggregation Operators · Grouping · Grouping, Aggregation, and Nulls · HAVING Clauses* | impresas **283–288** · PDF 320–325 | el `GROUP BY`/`HAVING` que el pipeline traduce; 6.4.6 explica por qué `$avg` ignora los ausentes |
| GMUW | **6.3.8** *Outerjoins* | impresa **277** · PDF 314 | el *left outer join* que `$lookup` implementa, con la semántica de `NULL` **opuesta** |
| GMUW | **5.2.4** *The Grouping Operator* · **5.2.7** *Outerjoins* | impresas **216**, **219** | los operadores del álgebra extendida; el pipeline es álgebra escrita en orden de ejecución |
| [[Corbellini et al (2017) - Persisting big-data — ficha\|Corbellini]] | **§ 6** · Table 6 | pp. **15–16** · p. **15** | *"For more advanced queries, that require to group data belonging to multiple documents, MongoDB can run MapReduce jobs"*: el paper **no conoce el pipeline** *(en 2016 ya existía)*; Table 6 lista como *query method* *consultas por campo, cursores y MapReduce* |
| [[Date - An Introduction to Database Systems — ficha\|Date]] 8ª ed. | **7.9** *Grouping and Ungrouping* | impresa **203** · PDF 230 | `GROUP`/`UNGROUP` sobre atributos relacionales: el análogo algebraico de `$push`/`$unwind`. **Solo el título verificado**: el cap. 7 del PDF no tiene OCR |
| [[Silberschatz - Fundamentos de Bases de Datos Cap 1 — ficha\|Silberschatz cap. 1]] | — | — | **Verificado que no hay** |
| *Practical MongoDB Aggregations*, Paul Done | *(nombrado en C14 s32; `practical-mongodb-aggregations.com`)* | — | **Sin ficha en el vault.** Es la única fuente en formato libro que un deck nombra sobre este tema |
| Documentación oficial | `reference/operator/aggregation-pipeline/` *(URL del slide 25 de la Clase 13)* | — | referencia de etapas; **única fuente** para `$lookup`, `$unwind`, versiones y límites |

**Lectura mínima:** Seven Databases **§ *Aggregated Queries*** *(impresas 115–117, 3 páginas)* y
después la **solución del handout de ecommerce** con la tabla del § 6 al lado. Para `$lookup` y
`$unwind` **no hay libro**: la documentación oficial y esta página.

## Dudas abiertas

- [ ] 🔴 **¿Entran `$lookup` y `$unwind` al parcial, o solo `$group`?** El TP9 pide `$group`
      *(ej. 10–11)* y nada más; los decks muestran `$lookup` dos veces y nunca `$unwind` en uso; la
      solución oficial del handout usa los tres en cada pipeline. Preguntar en la práctica del 22/09
      *(TP9 Parte II)*.
- [ ] 🔴 **Verificar en `mongosh` la semántica `null` = ausente de `$lookup`** con los datos del slide
      22 de la Clase 13: la orden 3 *(sin `item`)* debería salir con `inventory_docs` de **dos**
      elementos *(`_id` 5 y 6)*. La captura del slide 24 se corta antes. Es lo primero que conviene
      probar en el TP9 Parte II.
- [ ] 🔴 **Verificar en `mongosh` qué pasa con `$multiply` sin el segundo `$unwind`** del handout P1:
      según la documentación, con `"$producto.precio"` resuelto a un arreglo el pipeline **falla**
      *(`$multiply only supports numeric types, not array`)* y solo devuelve `null` cuando el campo
      falta o es `null`. No se pudo comprobar en esta sesión *(sin `mongosh` ni Docker corriendo)*;
      va junto con la prueba anterior en el TP9 Parte II.
- [ ] **¿Se dictó `$unwind` oralmente?** El slide 21 lo lista y ningún deck lo usa; sin él la
      solución del handout no se entiende. Si no se dictó, la fuente es esta página y la documentación.
- [ ] **`allowDiskUse` por defecto en 6.0** y la versión exacta de la imagen `mongo` sin tag del TP:
      afectan qué pasa cuando un `$group` supera los 100 MB. Marcado *a verificar* → [[MongoDB]].
- [ ] **¿Se archiva una ficha de *Practical MongoDB Aggregations*?** Es gratuito y en línea, el deck lo
      nombra y no hay otro libro con `$lookup`/`$unwind`. Decisión del humano *(va a
      [[_index-bibliografia]] § *Huecos*)*.
- [ ] **¿"Usando MongoDB (Compass)" en la consigna significa el constructor gráfico de pipelines?**
      Cambia la forma de entrega *(exportar desde Compass vs. escribir en `mongosh`)*, no el contenido.
- [ ] **¿Se dicta la forma `$lookup` con `let`/`pipeline`?** Es lo que hace falta para joins con
      condición distinta de la igualdad. Ningún deck la trae.

## Enlaces

- Clases: [[Clase 12 - Introduccion a NoSQL]] slides **46–49** *(primer `$group`, primer `$lookup`;
  y 50–52, la vista como pipeline con nombre)* · [[Clase 13 - NoSQL-EmbebidosVSNormalizado]] slides
  **21–25** *(la lista de etapas, `$lookup` sobre `orders`/`inventory`, la URL de referencia; y el 28
  para `explain`)* · [[Clase 14 - MongoDB Features]] slides **31–34** *(el diagrama oficial, `$sort`,
  `$project`, `$avg`, `count`/`distinct`)* y § *Material complementario* (a) *(los cinco pipelines
  del ecommerce)* y (c) *(el `mapReduce` traducido)*
- Práctica: [[Práctica 2026-09-15]] ejercicios **10–11** *(`$group` con `$avg` y `$sum: 1` sobre la
  vista `bandas_resumen`; el TP pide agregación sin enseñarla)*
- Conceptos hermanos de la U2: [[2.12.07 - CRUD y consultas en MongoDB|CRUD y consultas en MongoDB]]
  *(el `find()` cuya sintaxis `$match` reutiliza, y cuándo alcanza con él)* ·
  [[2.13.01 - Documentos embebidos vs. referencias|Documentos embebidos vs. referencias]] *(la
  decisión que determina si hace falta `$lookup` o `$unwind`)* ·
  [[2.13.02 - Relaciones 1:1, 1:N y N:M en MongoDB|Relaciones 1:1, 1:N y N:M en MongoDB]] *(las
  referencias que `$lookup` sigue)* ·
  [[2.12.06 - Modelo de documentos — JSON, BSON y ObjectId|Modelo de documentos]] *(los arreglos que
  `$unwind` desarma; el límite de 16 MB de la salida)* ·
  [[2.14.03 - MapReduce|MapReduce]] *(lo que el pipeline reemplaza; deprecado desde 5.0)* ·
  [[2.14.02 - Índices en MongoDB|Índices en MongoDB]] *(solo el `$match` inicial los usa)* ·
  [[2.14.01 - mongosh y herramientas de línea de comando|mongosh]] ·
  [[2.12.02 - Escalabilidad horizontal — sharding y replicación|Escalabilidad horizontal]]
  *(`$lookup` sobre colecciones *sharded*: desde 5.1)*
- Lo que traduce de la U1: [[1.05.01 - SQL — consultas|SQL — consultas]] §§ 1, 4, 5, 6 y 11 *(orden de
  evaluación, agregación, `GROUP BY`, `HAVING`, lógica trivaluada — la semántica de `null` del
  `$lookup` es la opuesta)* · [[1.06.01 - Vistas|Vistas]] *(la vista de MongoDB es un pipeline con
  nombre)* · [[1.08.01 - Plan de ejecución|Plan de ejecución]] *(`explain` sobre `aggregate`)* ·
  [[1.03.01 - Derivación de MER a esquema relacional|Derivación de MER a esquema relacional]] *(el
  join relacional que `$lookup` reemplaza)*
- Motores: [[MongoDB]] *(versión de la imagen del TP, límites, `allowDiskUse`, tabla shell legacy →
  `mongosh`)* · [[MySQL]] *(el `GROUP BY` con `ONLY_FULL_GROUP_BY` que `$group` vuelve obligatorio)*
- Bibliografía: [[_index-bibliografia]] › Clases 12, 13 y 14 · fichas:
  [[Seven Databases in Seven Weeks — ficha]] · [[Database Systems The Complete Book — ficha]] ·
  [[Corbellini et al (2017) - Persisting big-data — ficha]] ·
  [[Date - An Introduction to Database Systems — ficha]]
- Índices: [[_index-clases]] · [[_cronograma]] *(TP9 Parte II el 22/09)*
