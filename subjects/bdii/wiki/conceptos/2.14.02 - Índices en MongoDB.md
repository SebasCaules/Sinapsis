---
tipo: concepto
tema: Índices en MongoDB — el índice `_id` obligatorio, `createIndex`/`ensureIndex`, únicos, compuestos, multikey y sobre campos anidados, y `explain()` para ver si se usan
resumen: "Toda colección nace con un índice único sobre _id que no se puede borrar; los demás se crean con createIndex (ensureIndex es el nombre viejo): únicos, compuestos, anidados y multikey. Se mide con explain(): COLLSCAN vs. IXSCAN y totalDocsExamined; sin esquema, cuestan más que en relacional."
unidad: 2
clases: [14]
orden: 2
aliases:
  - Índices en MongoDB
  - Índice _id
  - createIndex
  - ensureIndex
  - dropIndex
  - getIndexes
  - Índice único en MongoDB
  - Índice compuesto en MongoDB
  - Índice multikey
  - Multikey index
  - Single field index
  - COLLSCAN
  - IXSCAN
  - explain en MongoDB
fuentes:
  - "raw/Unidad-02/Teorica/BD2_Clase 14 - MongoDB Features.pdf"
  - "raw/Unidad-02/Teorica/BD2_Clase 13 - NoSQL-EmbebidosVSNormalizado(1).pdf"
  - "raw/Unidad-02/Practica/ITBA TP 9 - MongoDB Parte I.pdf"
  - "raw/Unidad-02/Practica/ITBA TP 9 - MongoDB Parte II.pdf"
  - "raw/Unidad-02/Practica/mongoCities_fixed.json"
  - "raw/Examenes_Viejos/Drive bd2 (4to año 2Q)/Parciales viejos/Parcial_BDII_2Q2025_reconstruido(1).pdf"
  - "raw/Examenes_Viejos/Drive 72.41 - BDII - Examenes Viejos/BDII - Parcial 2Q2025.docx"
  - "raw/Examenes_Viejos/Drive bd2 (4to año 2Q)/Parciales viejos/parcial.pdf"
  - "raw/Examenes_Viejos/Drive ITBA Informatica - 72.41/Parciales/1Q2021.pdf"
  - "raw/Examenes_Viejos/Drive ITBA Informatica - 72.41/Parciales/2Q2021.pdf"
estado: procesado
---

# Índices en MongoDB

## Resumen general

Cubre los índices de MongoDB: el índice `_id` de toda colección, la sintaxis para crear, listar y borrar índices, los tipos que nombra la cursada (de un campo, anidado, compuesto, multikey, único y, desde el TP9 Parte II, el geoespacial `2d`) y `explain()` para verificar si una consulta usa índice. Importa para el parcial del 13/10 y el TP9: la Parte I (p. 6, pasos 30–34) lo ejercita sobre `players`, y la Parte II (ejercicios 2, 3 y 5) sobre `towns` y `cities`.

Reglas centrales: toda colección nace con un índice único sobre `_id`, no borrable, y es el único automático; el deck y el TP usan `ensureIndex`, deprecado desde 3.0 — hoy `createIndex`, mismos argumentos, aunque `mongosh` 2.11.1 todavía acepta `ensureIndex` sin aviso; rige el prefijo izquierdo en compuestos, y las direcciones (`1`/`-1`) importan para `sort`; un campo arreglo se vuelve *multikey* solo; `explain("executionStats")` equivale a `EXPLAIN` de MySQL (`COLLSCAN` vs. `IXSCAN` o `EXPRESS_IXSCAN`, `totalDocsExamined`); y sin esquema fijo los índices cuestan más que en relacional, porque un documento puede no tener el campo y cada consulta necesita el suyo.

Para el parcial: sintaxis de creación/borrado, `ensureIndex` vs. `createIndex`, leer `explain()`, los tipos, el `2d` con `$geoWithin`, y por qué el índice único es el único *constraint* declarativo de MongoDB. El Parcial 2Q2025 preguntó el índice por defecto: B-tree, automático, sobre `_id`.

> [!note] Concepto que **reencuadra** uno existente: es [[1.08.02 - Índices|Índices]] visto desde el otro motor
> Aparece por primera vez en el **slide 28** de la [[Clase 14 - MongoDB Features|Clase 14]]
> *("indexing — default _id index")*, con dos slides propios (28 y 29); el slide 30 ya es la tabla
> de herramientas de línea de comando *(va a [[2.14.01 - mongosh y herramientas de línea de comando|mongosh y herramientas]])*.
> La [[Clase 12 - Introduccion a NoSQL|Clase 12]] solo los nombra *(slide 29, "Soporte de
> índices"; el slide 2 habla de "indexado de documentos" como motivación genérica, no del
> mecanismo)*; la [[Clase 13 - NoSQL-EmbebidosVSNormalizado|Clase 13]] cierra con un
> `explain("executionStats")` *(slide 28)* como herramienta para verificarlos. El TP9 los ejercita
> en la **p. 6, pasos 30–34** *(`ensureIndex`, `dropIndex`, `unique`, compuesto, `explain`)*
> → [[Práctica 2026-09-15]].
>
> La teoría de estructuras *(B-tree, ordenado vs. asociativo)* no está en ningún deck de la U2: la
> dio la [[Clase 11 - Seguridad-Transacciones|Clase 11]] *(slides 31–38)* y vive en
> [[1.08.02 - Índices|Índices]], que esta página da por leída.

## Qué es

> [!quote] Clase 14, slide 28 — textual *(párrafo de la documentación oficial)*
> **indexing** · *default _id index*
> *"MongoDB creates a unique index on the _id field during the creation of a collection. […] You
> cannot drop this index on the _id field."*

> [!quote] TP9, p. 6 — textual
> *"Los índices en MongoDB funcionan muy parecido a los índices en una base de datos relacional,
> ayudan a mejorar el rendimiento de los queries y ordenados."*

> [!quote] *Seven Databases* 2ª ed., cap. 4 › Day 2 › § *Indexing: When Fast Isn't Fast Enough* *(impresa 110)*
> *"MongoDB provides several of the best data structures for indexing, such as the classic B-tree
> as well as other additions, such as two-dimensional and spherical GeoSpatial indexes."*

| Afirmación | De dónde sale | Qué implica |
| --- | --- | --- |
| Un índice es una **estructura aparte** que acelera la búsqueda por un campo | TP9 p. 6 | todo lo de [[1.08.02 - Índices\|Índices]] § *Lo que el deck sí demuestra* vale igual: full scan vs. acceso por índice, selectividad, prefijo izquierdo, cobertura |
| La estructura por defecto es un **B-tree** | *Seven Databases* impresa 110 | rangos, prefijos y ordenamientos lo aprovechan; igual que en InnoDB |
| Toda colección nace con **un índice único sobre `_id`** que **no se puede borrar** | Clase 14 slide 28 | la unicidad de `_id` es un índice, no una regla aparte; es el análogo de la clave primaria |
| Sirve también para **"ordenados"** | TP9 p. 6 | `sort()` sobre un campo indexado no necesita ordenar en memoria |

> [!important] (clave) La diferencia de fondo con lo relacional está en **el esquema**, no en el índice
> En MySQL el índice se declara sobre una **columna que existe** en todas las filas. En MongoDB se
> declara sobre un **nombre de campo**, y ningún documento está obligado a tenerlo: uno sin el campo
> entra con valor `null` *(salvo `sparse`)*. Es lo que hace que un índice **único** rechace el
> **segundo** documento sin el campo — y el motivo de que *Seven Databases* diga que los índices
> *"cuestan más en Mongo que en Postgres"* → § 5.

## 1 · El índice `_id`: el único que viene puesto *(Clase 14, slide 28)*

El slide 28 trae, además del párrafo de la documentación, el *script* del libro *(impresa 111)* para
listar todos los índices de la base:

```javascript
// Show all indexes of the current database
db.getCollectionNames().forEach(function(collection) {
  print("Indexes for the " + collection + " collection:");
  printjson(db[collection].getIndexes());
});
```

De paso: el shell es **JavaScript completo** *(`forEach`, funciones anónimas)*; `db[collection]` es
**acceso dinámico** a una colección por nombre; y `getIndexes()` es el `SHOW INDEX FROM tabla` de la
[[Clase 08 - Explicando el plan|Clase 08]].

`getIndexes()` sobre una colección recién creada devuelve **una sola entrada** *(impresa 113)*:

```javascript
{ "v" : 2, "key" : { "_id" : 1 }, "name" : "_id_", "ns" : "book.phones" }
```

> [!important] (clave) `_id_` es el **único** índice automático — verificado en MongoDB 8.3.11
> El TP9 Parte II lo pregunta tal cual *(ejercicio 2.b: "¿Hay índices creados automáticamente como
> ocurría en MySQL?")* y remite al libro, impresa 111: *"Whenever a new collection is created, Mongo
> automatically creates an index by the _id. These indexes can be found in the system.indexes
> collection."* La segunda frase quedó vieja: `system.indexes` ya no se consulta; hoy se usa
> `getIndexes()` por colección. Salida real sobre `towns`, recién creada y sin pedir ningún índice
> *([[Práctica 2026-09-22|Práctica 2026-09-22]] § Ejercicio 2)*:
> ```javascript
> db.towns.getIndexes()
> // [ { v: 2, key: { _id: 1 }, name: '_id_' } ]
> ```
> Una sola entrada, ya sin el campo `ns` que imprime el libro. La diferencia con MySQL: allí el índice
> automático es el de la columna declarada `PRIMARY KEY`, sea cual sea, y InnoDB crea además uno por
> cada `UNIQUE` y por cada clave foránea que no tenga ya un índice utilizable *(verificado en MySQL
> 9.7.2: `PRIMARY KEY`, `UNIQUE (cod)` y `FOREIGN KEY (pid)` dejan tres índices sin ningún
> `CREATE INDEX`)*; aquí es siempre `_id`, el único campo obligatorio de todo documento. Lo que el usuario agrega después (`unique`, compuestos,
> `2d`) se suma a esa entrada, nunca la reemplaza.

| Propiedad del índice `_id` | Consecuencia |
| --- | --- |
| Se crea **con la colección**, sin pedirlo | `insertOne` en una colección inexistente la crea **con su índice** |
| Es **único** | el segundo `_id` repetido falla con `E11000 duplicate key error` — el slide 9 de la Clase 14 lo mostraba al revés: tres tipos de `_id`, ninguno repetido |
| **No se puede borrar** | `dropIndex("_id_")` es error; `dropIndexes()` borra todos **menos** ese |
| Se llama **`_id_`** | convención `campo_dirección`: `_id` + `_1` sin el `1` |
| El `v: 2` es la **versión del formato** de índice | desde 3.4; no hay que tocarlo |

> [!tip] Es el análogo de la clave primaria, con una diferencia que importa para el modelado
> En InnoDB la clave primaria **es** la tabla *(índice clustered)*. En MongoDB `_id` es un índice
> **secundario más**: los documentos viven aparte en WiredTiger. No hay clustered por defecto en la
> versión de la cursada *(existen las *clustered collections* desde 5.3, sin mención en el vault)*.
> Para el parcial alcanza con: **`_id` = clave primaria = índice único automático**.

## 2 · Crear, listar y borrar *(Clase 14 slide 29 · TP9 pasos 30–33)*

> [!quote] Clase 14, slide 29 — textual
> **single field indexes**
> ```
> > db.phones.count()
> 100,000 rows
>
> > db.phones.find({display: "+1 800-5650001"}).explain()  → 61 ms
> > db.phones.ensureIndex( { display : 1 }, { unique : true } )
> > db.phones.find({display: "+1 800-5650001"}).explain()  → 0 ms
>
> // index on nested values
> > db.phones.ensureIndex({ "components.area": 1 }, { background : 1 })
> ```
> *"+info: https://docs.mongodb.com/manual/core/index-single/"*

> [!quote] TP9, p. 6 — pasos 30–33, textuales (resumidos)
> *"30. Los índices son creados con el comando **ensureIndex**: `db.players.ensureIndex({name:1})`"*
> *"31. Los índices pueden ser borrados usando **dropIndex**: `db.players.dropIndex({name:1})`"*
> *"32. Un índice único se crea utilizando el parámetro **unique**:
> `db.players.ensureIndex({name:1},{unique:true})`"*
> *"33. […] pueden ser creados en campos embebidos (usando la notación de punto) y en campos de
> arreglos. También, es posible crear índices compuestos: `db.players.ensureIndex({name:1, weight:1})`"*

### 2.1 · La firma: `createIndex(claves, opciones)`

```javascript
db.coleccion.createIndex( { campo: 1 | -1, ... } , { unique: true, name: "...", sparse: true, ... } )
```

| Parte | Qué es | Notas |
| --- | --- | --- |
| **`claves`** | documento `{ campo: dirección }` | `1` ascendente, `-1` descendente. En un índice de **un** campo la dirección da igual; en uno **compuesto**, importa para `sort` → § 3.3 |
| **`opciones`** | documento opcional | `unique`, `name`, `sparse`, `partialFilterExpression`, `expireAfterSeconds` *(TTL)*, `background` *(ignorado desde 4.2)* |
| **devuelve** | el **nombre** del índice | `'name_1'`, `'name_1_weight_1'`, `'components.area_1'`: campo + `_` + dirección |

### 2.2 · El vocabulario completo, y lo que está deprecado

| Operación | Como está en el deck / TP | Como se escribe hoy en `mongosh` | Estado |
| --- | --- | --- | --- |
| crear | `ensureIndex(claves, opciones)` *(slide 29; TP9 pasos 30, 32, 33)* | **`createIndex(claves, opciones)`** — mismos argumentos | (crítico) `ensureIndex` **deprecado desde 3.0**, fuera del manual actual *(verificado el 15/09 contra la lista de métodos de colección)*; que `mongosh` lo conserve como alias con advertencia es entendimiento propio, no probado en el motor. (ok) **Probado en MongoDB 8.3.11 / `mongosh` 2.11.1**: corre, crea el índice y **no imprime ninguna advertencia**; devuelve un arreglo (`[ 'name_1' ]`) donde `createIndex` devuelve el string `'name_1'` *([[Práctica 2026-09-22\|Práctica 2026-09-22]] § Qué del TP está deprecado)* |
| crear varios | — | `createIndexes([ {…}, {…} ])` | ✓ |
| listar | `getIndexes()` *(slide 28)* | igual | ✓ |
| borrar uno | `dropIndex({name:1})` *(TP9 paso 31)* | igual; acepta la **especificación** o el **nombre** | ✓ |
| borrar todos | — | `dropIndexes()` — todos **menos `_id_`** | ✓ |
| esconder sin borrar | — | `hideIndex` / `unhideIndex` *(4.4)*: el planificador deja de verlo pero sigue manteniéndose; prueba si un índice hace falta sin borrarlo | ✓ |
| tamaño | — | `totalIndexSize()` · `db.coleccion.stats().indexSizes` | ✓ |
| construir sin bloquear | `{ background : 1 }` *(slide 29)* | *(omitir)* | (crítico) **ignorado desde 4.2**: todos los índices se construyen sin bloquear la colección |
| contar antes de medir | `db.phones.count()` *(slide 29)* | `countDocuments()` / `estimatedDocumentCount()` | `count()` deprecado en `mongosh` |

> [!warning] (crítico) El slide 29 dice *"100,000 **rows**"* — y en MongoDB no hay filas
> Es la única vez en los tres decks que aparece la palabra *rows* para hablar de documentos. Herencia
> del libro *(que dice "100,000 phone numbers")*; no cambia nada técnico, pero conviene no
> repetirlo en un parcial cuya pregunta favorita es *"¿en qué se diferencia una colección de una
> tabla?"* → [[2.12.06 - Modelo de documentos — JSON, BSON y ObjectId|Modelo de documentos]]. A favor
> del deck: el libro escribe `{ unique : true, dropDups : true }` *(impresa 111)* y **`dropDups` fue
> eliminado en 3.0** — el slide lo quita.
>
> En MongoDB 8.3.11 la opción **se acepta en silencio y no hace nada**: sobre una colección con un
> valor repetido, `ensureIndex({name:1}, {unique:true, dropDups:true})` falla con `DuplicateKey`
> (`E11000`) igual que sin la opción, y los documentos siguen todos ahí. No hay reemplazo: los
> duplicados se limpian a mano antes de crear el índice único *([[Práctica 2026-09-22|Práctica 2026-09-22]] § Qué del TP
> está deprecado; reproducido también en esta página contra el mismo motor)*.

### 2.3 · Dos errores de secuencia que el TP9 provoca

| Situación | Qué pasa | Cómo se evita |
| --- | --- | --- |
| Paso 32 *(`{name:1}, {unique:true}`)* **sin** haber corrido el 31 | `IndexOptionsConflict`: ya existe un índice con las mismas claves y otras opciones | ejecutar el 31 antes, o `dropIndexes()` |
| `unique:true` sobre una colección con **valores repetidos** | `E11000 duplicate key error` — el índice **no se crea** | en el TP no ocurre: los 12 nombres son distintos |
| Índice único y documentos **sin el campo** | el primero entra con `null`; el **segundo** sin el campo viola la unicidad | `sparse: true` o `partialFilterExpression` — el TP9 no los usa; Kane sin `height` es el caso donde importaría |

> [!note] Es el único *constraint* declarativo que MongoDB ofrece
> Parecido a `UNIQUE` de la [[1.09.01 - Restricciones de integridad|Clase 09]]. **No hay `NOT NULL`,
> ni `CHECK`, ni claves foráneas** *(existe `$jsonSchema`, fuera de la cursada)*: el resto de la
> integridad vive en la aplicación o el modelado → [[2.13.01 - Documentos embebidos vs. referencias|Documentos embebidos vs. referencias]].

## 3 · Los tipos que la cursada nombra

Cinco, entre el slide 29 y el paso 33. Ninguno explicado por dentro: todos son B-tree salvo que se
pida otra cosa.

### 3.1 · De un campo *(single field)* — slide 29

`{ display : 1 }`. Es el caso base y el del experimento del libro: 100.000 documentos, búsqueda por
igualdad, *collection scan* completo antes y una sola lectura después → § 4.

### 3.2 · Sobre un campo anidado — slide 29 y TP9 paso 33

```javascript
db.phones.createIndex({ "components.area": 1 })  // slide 29, sin el background
db.players.createIndex({ "team.team_short_name": 1 })  // TP9 paso 33, ejemplo propio
```

**Notación de punto**, entre comillas. **No tiene equivalente relacional directo**: en MySQL un dato
anidado sería otra tabla con su propio índice; aquí el índice entra hasta el subdocumento. Se
combina con `find({'team.team_short_name': 'JUV'})` (paso 29 del TP9).

### 3.3 · Compuesto — TP9 paso 33

```javascript
db.players.createIndex({ name: 1, weight: 1 })  // → 'name_1_weight_1'
```

Vale **la regla del prefijo izquierdo** que [[1.08.02 - Índices|Índices]] § 3 enunció:
`{name:1, weight:1}` sirve para `name` y para `name + weight`; **no** para `weight` solo. Lo nuevo:
en un compuesto **las direcciones importan para `sort`** — `{ name: 1, weight: -1 }` soporta
`sort({name: 1, weight: -1})` y su inverso exacto, pero no `sort({name: 1, weight: 1})`.

### 3.4 · Multikey — TP9 paso 33, *"campos de arreglos"*

```javascript
db.players.createIndex({ hobbies: 1 })  // hobbies es un arreglo
```

El TP lo nombra y no lo muestra. Sobre un campo **arreglo**, MongoDB crea **una entrada por
elemento**: permite que `{hobbies: 'Swimming'}` *(paso 27 del TP9)* use índice, donde MySQL exigiría
una tabla de detalle. El motor lo decide solo al ver el primer arreglo, sin que se lo pida. Un
índice compuesto puede tener **a lo sumo un** campo arreglo.

### 3.5 · Único — slide 29 y TP9 paso 32

`{ unique : true }`, ya visto en § 2.3. Se agrega que **puede combinarse** con compuesto *(unicidad
sobre la tupla)* y con multikey *(ningún elemento del arreglo se repite entre documentos)*.

### 3.6 · Geoespacial `2d` — TP9 Parte II, ejercicio 5

El sexto tipo entra por la práctica, no por un deck: el ejercicio 5 del TP9 Parte II pide cargar
`mongoCities_fixed.json` *(99.838 ciudades)*, *"definir el índice 2d en el campo location como indica
la página 130"* y resolver el Do.1 del *Day 3*: las ciudades a 50 millas del centro de Londres.

> [!quote] *Seven Databases* 2ª ed., cap. 4 › Day 3 › § *GeoSpatial Queries* *(impresa 130)*
> *"The core of the geospatial secret lies in indexing. It's a special form of indexing geographic data
> called geohash […]. The 2d index must be set on any two value fields, in our case a hash (for
> example, { longitude:1.48453, latitude:42.57205 }), but it could easily have been an array (for
> example [1.48453, 42.57205])."* — `> db.cities.ensureIndex({ location : "2d" })`

```javascript
db.cities.createIndex({ location: "2d" })            // → 'location_2d'
db.cities.getIndexes().map(i => i.name)              // → [ '_id_', 'location_2d' ]

// Do.1 del Day 3: ciudades a 50 millas del centro de Londres
db.cities.countDocuments({
  location: { $geoWithin: { $centerSphere: [ [-0.1276, 51.5072], 50 / 3963.2 ] } }
})                                                   // → 297
```

*(Salidas reales en MongoDB 8.3.11, sobre el archivo de `raw/`; la lista de las primeras quince
ciudades está en [[Práctica 2026-09-22|Práctica 2026-09-22]] § Ejercicio 5.)*

| Pieza | Qué es | Detalle verificado |
| --- | --- | --- |
| `{ location: "2d" }` | índice sobre un **par de coordenadas** *(arreglo `[x, y]` o subdocumento de dos campos)* | el valor de la clave es la cadena `"2d"`, no `1`/`-1`; el nombre sale igual por convención: `location_2d` |
| `$geoWithin` + `$centerSphere` | "dentro de un círculo", en un `find` común | el radio va en **radianes**: millas ÷ 3963,2 *(radio terrestre en millas)*; para kilómetros, ÷ 6378,1 |
| ¿usa el índice? | sí | `explain()` del `find` anterior: `FETCH` sobre `IXSCAN` con `indexName: 'location_2d'` |
| `$near` | los más cercanos, ordenados por distancia | `find({ location: { $near: [-0.1276, 51.5072] } }).limit(3)` → `London`, `City of Westminster`, `Holborn` |
| `$geoNear` | lo mismo como **etapa de `aggregate`**, con la distancia en un campo | tiene que ser la **primera etapa** del pipeline; es lo que usa el libro *(impresa 130)* para Portland |

> [!warning] (crítico) El orden de las coordenadas: el dataset `_fixed` las mezcla
> MongoDB lee el par como **`[longitud, latitud]`**. En `mongoCities_fixed.json` Londres (GB),
> Portland (US), Buenos Aires y Sydney están cargadas así, pero París, Andorra la Vella, Tokio y Moscú
> están como `[latitud, longitud]`, que es el orden del ejemplo del libro (`near: [45.52, -122.67]`).
> La consulta de Londres da bien porque las ciudades vecinas del Reino Unido comparten convención;
> centrada en París daría un resultado silenciosamente incorrecto. Además, hay varias `London` en el
> archivo: `findOne({ name: "London" })` devuelve la de Canadá (`country: 'CA'`); la inglesa se pide
> con `country: "GB"` → `[-0.12574, 51.50853]`. Detalle de las ocho ciudades, con sus coordenadas, en
> [[Práctica 2026-09-22|Práctica 2026-09-22]] § *Ejercicio 5*.

> [!note] `2d` vs. `2dsphere`
> El TP usa `2d`, el índice para **pares de coordenadas sueltos** *(legacy coordinate pairs)*, que es
> el formato del dataset del libro. Para documentos **GeoJSON** (`{ type: "Point", coordinates: [lon,
> lat] }`) el índice es `2dsphere`, con el que `$geoNear` recibe un punto GeoJSON y `maxDistance` en
> metros —la forma de la resolución del vault de la pregunta 10 del [[Parcial 2Q2021|Parcial 2Q2021]]—. Ningún deck ni TP de la
> cursada muestra `2dsphere`: es contexto del motor, según la documentación oficial.

### Lo que **no** está en ningún material de la cursada

| Tipo | Para qué | Dónde se menciona |
| --- | --- | --- |
| **de texto** *(`"text"`)* | búsqueda por palabras; un índice por colección | en ningún lado — relacionado: el `$regex` del TP9 *(pasos 14–15)* sin `^` recorre todas las claves del índice, mejor que un `COLLSCAN` pero no una búsqueda por prefijo |
| **geoespacial** *(`2dsphere`)* | `$near`, `$geoWithin` | *Seven Databases* Day 3 § *GeoSpatial Queries* *(impresas 130–131)*; el dataset `hospitales` del slide 33 de la Clase 14 es GeoJSON, sin explotarlo. **El `2d` sí está en la cursada** desde el TP9 Parte II → § 3.6 |
| **TTL** *(`expireAfterSeconds`)* | borrar documentos viejos solos | en ningún lado |
| **hashed** | *sharding* por hash de la *shard key* | de costado: el slide 8 de la Clase 12 dibuja el hash sin nombrar el índice → [[2.12.02 - Escalabilidad horizontal — sharding y replicación\|Sharding y replicación]] |
| **parcial / sparse** | indexar solo documentos que cumplen un filtro / que tienen el campo | en ningún lado; es la respuesta al caso de Kane sin `height` |
| **wildcard** | campos desconocidos de antemano | en ningún lado |

## 4 · Medir si se usa: `explain()` *(Clase 14 slide 29 · Clase 13 slide 28 · TP9 paso 34)*

Los tres materiales cierran el tema igual: **medir antes, indexar, medir después**. Es el método de
la [[Práctica 2026-08-18]] *(TP5, `EXPLAIN` en MySQL)* con otra sintaxis.

> [!quote] Clase 13, slide 28 — textual *(último slide del deck)*
> *"[…] runs `cursor.explain()` in `"executionStats"` verbosity mode to return the query planning
> and execution information for the specified `db.collection.find()` operation:"*
> ```javascript
> db.products.find(
> { quantity: { $gt: 50 }, category: "apparel" }
> ).explain("executionStats")
> ```

> [!quote] TP9, paso 34 — textual
> *"34. Para validar si los queries están usando índices […], se puede utilizar el método explain,
> al final de la consulta. […] veremos con la siguiente query que figura el nombre del índice que es
> usado: `db.players.find({name: 'Lionel Messi'}).explain()`"*

| | `explain()` de MongoDB | `EXPLAIN` de MySQL *(U1)* |
| --- | --- | --- |
| Dónde va | **encadenado al cursor**: `find(...).explain(...)` | **delante** de la sentencia: `EXPLAIN SELECT …` |
| Niveles | `"queryPlanner"` *(default)* · **`"executionStats"`** · `"allPlansExecution"` | `EXPLAIN` · `EXPLAIN ANALYZE` |
| Full scan | `stage: 'COLLSCAN'` | `type: ALL` |
| Acceso por índice | `stage: 'IXSCAN'` con `indexName` y `keyPattern`, envuelto en `FETCH` | `type: ref / range / eq_ref / const`, `key: nombre_del_índice` |
| Cobertura *(no leer el documento)* | `IXSCAN` **sin** `FETCH`, con `PROJECTION_COVERED` | `Extra: Using index` |
| Lo que agrega `executionStats` | `nReturned`, **`totalDocsExamined`**, `totalKeysExamined`, `executionTimeMillis` | lo que agrega `ANALYZE`: filas reales y tiempos |

**Lo que hay que buscar** en la salida del paso 34, con el índice del paso 32 puesto:

```javascript
queryPlanner: {
  winningPlan: {
  stage: 'FETCH',
  inputStage: {
  stage: 'IXSCAN',
  keyPattern: { name: 1 },
  indexName: 'name_1',  // ← "el nombre del índice que es usado"
  isUnique: true,
  indexBounds: { name: [ '["Lionel Messi", "Lionel Messi"]' ] }
  }
  }
}
```

Sin el índice, el `winningPlan` es `{ stage: 'COLLSCAN', filter: {…} }`. Con
`explain("executionStats")` la diferencia se vuelve número: `totalDocsExamined` **109 999 → 1** en
el libro, y **12 → 1 esperable** en el TP9 *(el enunciado no imprime salida)*.

> [!important] (atención) En MongoDB 8.3.11 la búsqueda por igualdad sobre un índice único da **`EXPRESS_IXSCAN`**
> El plan de arriba *(`FETCH` sobre `IXSCAN`)* es el del libro y el de la predicción a mano de la
> Parte I. La corrida real del TP9 Parte II, ejercicio 3 *(índice único en `name`, `find({ name:
> 'Portland' })`)* dio otra cosa ([[Práctica 2026-09-22|Práctica 2026-09-22]] § Ejercicio 3):
> ```javascript
> executionStages: { stage: 'EXPRESS_IXSCAN', keyPattern: '{ name: 1 }', indexName: 'name_1',
>                    keysExamined: 1, docsExamined: 1, nReturned: 1 }
> ```
> Es una vía de ejecución para consultas puntuales *(igualdad sobre `_id` o sobre un índice único)*
> que resuelve la búsqueda sin el `FETCH` envolvente. Se lee igual que `IXSCAN`: **usó el índice**,
> con `indexName` y `totalDocsExamined: 1`. Reproducido también sobre `_id`
> *([[Parcial 23-5-23 - Bases de Datos Avanzadas|Parcial 23-5-23 - Bases de Datos Avanzadas]], pregunta 32: `indexName: '_id_'`)*. Una consulta
> por rango o sobre un índice no único sigue mostrando `FETCH` + `IXSCAN` *(verificado en el mismo
> motor: igualdad y rango sobre un índice no único `a_1`, y el `2d` del § 3.6)*. Para el parcial, los dos nombres valen como "acceso por índice"; el que indica full scan
> sigue siendo solo `COLLSCAN`.

> [!important] (clave) Los tres números del experimento *(61 ms / 52 ms / 12 → 1)* y por qué no coinciden
> El cruce del slide 29 de la Clase 14, *Seven Databases* impresas 111–112 y el paso 34 del TP9 vive
> en [[1.08.01 - Plan de ejecución|Plan de ejecución]] § *Anatomía de la salida — `executionStats`*.
> **La lección no es el milisegundo: es `COLLSCAN` → `IXSCAN` y `totalDocsExamined`.** Con 12
> jugadores el tiempo no debería moverse y el índice **igual** se está usando.

> [!warning] Dos trampas del paso 34 del TP9
> 1. El enunciado dice *"si se mantiene el índice del punto 32"*, pero el **paso 33 crea otro**
> *(`name_1_weight_1`)*, y **los dos sirven** para `{name: 'Lionel Messi'}`; con 12 documentos el
> planificador puede elegir cualquiera *(no verificado)* → § *Dudas abiertas*.
> 2. **La forma de la salida depende de la versión.** En algunas series `winningPlan` viene envuelto
> en `queryPlan`/`slotBasedPlan`: si no coincide con el ejemplo, buscar `IXSCAN` donde esté.
> `docker pull mongo` sin *tag* no fija la versión.

El detalle del plan —etapas, verbosidades, `allPlansExecution`— va en
[[1.08.01 - Plan de ejecución|Plan de ejecución]] § *`explain()` en MongoDB*; aquí solo lo que hace
falta para decidir si un índice hace falta.

## 5 · Lo que cuesta un índice — y por qué cuesta **más** aquí

> [!quote] *Seven Databases* 2ª ed., impresa 114 — el párrafo que el slide 29 omite
> *"[…] creating an index on a large collection can be slow and resource-intensive. Indexes simply
> "cost" more in Mongo than in a relational database like Postgres due to Mongo's schemaless nature.
> […] creating indexes at off-peak times, running index creation in the background, and running
> them manually rather than using automated index creation."*

> [!quote] Corbellini et al. 2017, § 6, p. 14 — el mismo costo, desde la teoría
> *"[…] queries can be done on any field using patterns. […] The drawback is that for each type of
> query a new index needs to be created, because document databases index elements by the document
> identifier."*

| Costo | Por qué | Cómo se paga |
| --- | --- | --- |
| **Escritura**: cada `insert`/`update`/`delete` actualiza **todos** los índices de la colección | igual que en relacional; § 7 de [[1.08.02 - Índices\|Índices]] *("más índices no es mejor")* | no indexar lo que no se consulta |
| **Sin esquema, sin garantías**: el motor no sabe de antemano qué documentos tienen el campo, de qué tipo ni si es arreglo | *"schemaless nature"* | el índice se construye recorriendo **toda** la colección, documento por documento; multikey se detecta al vuelo |
| **Un índice por tipo de consulta** | Corbellini: sin índice, MongoDB busca por `_id`; cada patrón de consulta distinto necesita el suyo | modelar las consultas **antes** que los documentos — *"Hint: antes de definir el modelo revise las consultas"* del ejercicio 1 del TP9 |
| **Construcción sobre colección grande** | lenta y consume recursos | horarios de baja carga; hoy ya no hace falta `background` *(4.2)* |
| **Memoria**: el índice rinde si el *working set* de sus páginas entra en RAM | B-tree en disco; WiredTiger lo cachea | `totalIndexSize()` para saber cuánto ocupa |

> [!tip] La regla de decisión es la misma de la U1, con un agregado
> Se indexa lo que se **filtra**, se **ordena** o se **une** *(`$lookup` usa el índice de la
> colección foránea si existe)*. Se agrega: la **shard key** lleva índice obligatorio *(lo primero
> que verifica `sh.shardCollection`)* → [[2.12.02 - Escalabilidad horizontal — sharding y replicación|Sharding y replicación]].
> Y se resta: lo **embebido** ya no necesita índice propio en otra colección →
> [[2.13.01 - Documentos embebidos vs. referencias|embebido vs. referenciado]].

## 6 · Contraste con el motor de la U1

| | MySQL / InnoDB *(U1)* | MongoDB *(U2)* |
| --- | --- | --- |
| Índice automático | la **clave primaria** *(clustered: la tabla es el árbol)* | el índice **`_id_`** *(único, no borrable, secundario)* |
| Crear · borrar · listar | `CREATE INDEX nombre ON tabla (cols);` · `ALTER TABLE … ADD INDEX` · `DROP INDEX nombre ON tabla;` *(el `ON tabla` es obligatorio)* · `SHOW INDEX FROM tabla;` | `db.col.createIndex({campo:1},{opciones})` · `dropIndex({campo:1})` o por nombre · `getIndexes()` |
| Nombre | lo pone el usuario | lo arma el motor: `campo_1_otro_-1` *(o `name:` en opciones)* |
| Único | `CREATE UNIQUE INDEX` · `UNIQUE` en el DDL | `{ unique: true }` |
| Estructura por defecto | B-tree | B-tree |
| Hash | `USING HASH` **ignorado** por InnoDB *(Clase 11, slide 38)* | `{ campo: "hashed" }` — existe, sirve para *sharding* |
| Sobre un campo anidado / arreglo | no aplica *(sería otra tabla / tabla de detalle)* | notación de punto `{ "a.b": 1 }` / **multikey** automático |
| Compuesto y prefijo izquierdo | ✓ igual | ✓ igual; las direcciones importan para `sort` |
| Índice sobre campo **ausente** | imposible: toda fila tiene la columna | entra con `null` *(salvo `sparse`)* |
| Medir | `EXPLAIN SELECT …` → `type`, `key`, `Extra` | `find(…).explain("executionStats")` → `stage`, `indexName`, `totalDocsExamined` |
| Otros *constraints* declarativos | `NOT NULL`, `CHECK`, `FOREIGN KEY` | **ninguno**: solo el índice único *(y `$jsonSchema`, fuera de la cursada)* |

> [!note] Lo que se transfiere entero de la U1
> Los cuatro conceptos que [[1.08.02 - Índices|Índices]] marcó como independientes del motor valen
> aquí sin cambios: **full scan vs. acceso por índice**; condición que **acota el acceso** vs.
> condición que **filtra después** *(`IXSCAN` + `FETCH` con `filter`)*; **orden de los campos** en un
> compuesto; e índice que **cubre** la consulta *(`PROJECTION_COVERED`)*. Y una quinta regla: **`$regex`
> sin `^` al inicio no puede acotar el rango** —recorre todas las claves del índice, mejor que un
> `COLLSCAN` pero no una búsqueda por prefijo—, por el mismo motivo que `LIKE '%abc'` no acota el
> rango en MySQL: un B-tree se camina por prefijo.

## 7 · En el motor de la cursada — `mongosh` sobre la imagen `mongo` de Docker

Todo lo de esta página **corre en `mongosh`** sobre el contenedor del TP9, con dos sustituciones que
no cambian el resultado:

```javascript
// TP9, pasos 30–34, como se escriben hoy
db.players.createIndex({ name: 1 })  // 30  → 'name_1'
db.players.dropIndex({ name: 1 })  // 31  → { nIndexesWas: 2, ok: 1 }
db.players.createIndex({ name: 1 }, { unique: true })  // 32  → 'name_1'
db.players.createIndex({ name: 1, weight: 1 })  // 33  → 'name_1_weight_1'
db.players.createIndex({ 'team.team_short_name': 1 })  //  embebido (nombrado, no mostrado)
db.players.createIndex({ hobbies: 1 })  //  multikey (nombrado, no mostrado)
db.players.find({ name: 'Lionel Messi' }).explain()  // 34  → buscar IXSCAN e indexName
db.players.find({ name: 'Lionel Messi' }).explain('executionStats').executionStats
  //  → totalDocsExamined: 1
db.players.getIndexes()  //  → _id_, name_1, name_1_weight_1, …
```

*(Propuesta propia sobre el enunciado; el `nIndexesWas: 2` del paso 31 cuenta el `_id_` más el
`name_1` del paso 30. Resultados calculados sobre los datos del TP, no verificados contra el motor.)*

| Lo que puede fallar | Síntoma | Corrección |
| --- | --- | --- |
| `ensureIndex` no existe en el `mongosh` de la imagen | `TypeError: … ensureIndex is not a function` | `createIndex`, mismos argumentos |
| `ensureIndex` existe pero avisa | `DeprecationWarning` y sigue | nada; anotar la versión con `db.version()` |
| `ensureIndex` existe y **no avisa** *(lo verificado en MongoDB 8.3.11 / `mongosh` 2.11.1)* | crea el índice y devuelve `[ 'name_1' ]` | nada que corregir para que corra; `createIndex` igual es la forma documentada |
| `dropDups: true` del libro | se acepta sin error y no borra nada: con duplicados, `E11000` igual | limpiar duplicados antes del índice único |
| búsqueda por igualdad sobre índice único | `explain()` muestra `EXPRESS_IXSCAN`, sin `FETCH` | es acceso por índice → § 4 |
| paso 32 sin el 31 | `IndexOptionsConflict` | `dropIndex({name:1})` o `dropIndexes()` |
| la salida de `explain()` no se parece al ejemplo | claves en otro lugar *(`queryPlan`, `slotBasedPlan`)* | buscar `IXSCAN` / `COLLSCAN` donde esté; la forma depende de la versión |
| `count()` en el slide 29 | `DeprecationWarning` | `countDocuments()` |

La versión exacta que trae `docker pull mongo` hoy **no se puede verificar desde el vault** →
[[MongoDB]] § *Versión* y § *Dudas abiertas*.

## Para el parcial

| Pregunta probable | Respuesta corta, con cita |
| --- | --- |
| ¿Qué índice tiene toda colección de MongoDB? | uno **único sobre `_id`**, creado con la colección, que **no se puede borrar** *(Clase 14, slide 28)* |
| ¿Cómo se crea un índice y cómo se borra? | `createIndex({campo: 1}, {opciones})` · `dropIndex({campo: 1})` · se listan con `getIndexes()` *(slide 28; TP9 pasos 30–31)*. Si se escribe `ensureIndex`, aclarar que es el nombre viejo |
| ¿Qué estructura usa? | **B-tree**, como InnoDB *(Seven Databases impresa 110)*; sirve para igualdad, rango, prefijo y orden |
| ¿Qué tipos hay? | de un campo · sobre campo anidado *(notación de punto)* · compuesto · **multikey** *(arreglos)* · único *(slide 29; TP9 paso 33)* · geoespacial **`2d`** *(TP9 Parte II, ej. 5 → fila siguiente)*. Fuera de la cursada: texto, geoespacial `2dsphere`, TTL, hashed, parcial |
| ¿Cómo se buscan ciudades en un radio? | `createIndex({ location: "2d" })` y `find({ location: { $geoWithin: { $centerSphere: [ [lon, lat], millas / 3963.2 ] } } })`; Londres, 50 millas → **297** ciudades *(TP9 Parte II ej. 5; Seven Databases impresas 130 y 132)*. Trampa: el par va `[lon, lat]` |
| ¿Hay índices creados automáticamente, como en MySQL? | Sí, **solo uno**: `_id_`, B-tree, único, sobre `_id` *(TP9 Parte II ej. 2; impresa 111; `getIndexes()` real)*. Es la pregunta 9 del [[Parcial 2Q2025\|Parcial 2Q2025]] |
| ¿Cómo se sabe si una consulta usa un índice? | `find(…).explain("executionStats")`: `IXSCAN` vs. `COLLSCAN`, `indexName`, `totalDocsExamined` *(Clase 13 slide 28; TP9 paso 34)* |
| ¿Qué muestra el experimento de `phones`? | 100.000 documentos: `find` por igualdad **61 ms → 0 ms** al crear el índice sobre `display` *(slide 29)*; el libro: 109 999 → 1 documentos examinados |
| ¿Cuál es el único *constraint* declarativo de MongoDB? | el **índice único** — no hay `NOT NULL`, `CHECK` ni claves foráneas *(TP9 paso 32; razonamiento del vault)* |
| ¿Por qué un índice "cuesta más" en MongoDB que en un relacional? | por la **falta de esquema**: el motor no sabe qué documentos tienen el campo ni de qué tipo, y cada tipo de consulta necesita el suyo *(Seven Databases impresa 114; Corbellini § 6, p. 14)* |
| ¿Qué regla del compuesto vale igual que en SQL? | **prefijo izquierdo**: `{name:1, weight:1}` sirve para `name` y `name+weight`, no para `weight` solo *(TP9 paso 33; Clase 08)* |
| ¿Qué índice no tiene equivalente relacional? | **multikey**: una entrada por elemento del arreglo; en SQL sería una tabla de detalle |

## Cuadro de bolsillo

| Pregunta | Respuesta |
| --- | --- |
| El índice que viene puesto | `_id_`, único, no borrable |
| Crear · listar · borrar | `createIndex` · `getIndexes` · `dropIndex` / `dropIndexes` |
| El nombre viejo | `ensureIndex` *(deprecado 3.0; el deck y el TP9 lo usan)* |
| Dirección | `1` asc · `-1` desc; importa solo en compuestos, para `sort` |
| Opciones que la cursada usa | `unique: true` · `background: 1` *(ignorada desde 4.2)* |
| Tipos nombrados | un campo · anidado *(`"a.b"`)* · compuesto · multikey · único · geoespacial `2d` *(TP9 Parte II)* |
| Geoespacial | `{ location: "2d" }` → `location_2d`; `$geoWithin` + `$centerSphere` *(radio en radianes)*, `$near`; `$geoNear` solo como primera etapa de `aggregate` |
| Stage de consulta puntual | `EXPRESS_IXSCAN` *(igualdad sobre `_id` o índice único, MongoDB 8.3.11)* = acceso por índice |
| Nombre automático | `campo_1`, `campo_1_otro_1`, `"a.b"_1` → `a.b_1` |
| Medir | `.explain("executionStats")` → `IXSCAN` / `COLLSCAN`, `totalDocsExamined` |
| Regla del compuesto | prefijo izquierdo |
| `$regex` e índice | sin `^` recorre todas las claves del índice *(`IXSCAN` completo)*; con `^` acota el rango |
| Por qué cuestan más | sin esquema; un índice por tipo de consulta |
| Errores típicos | `E11000` *(duplicado con `unique`)* · `IndexOptionsConflict` *(mismas claves, otras opciones)* |

## Bibliografía verificada

Todo lo de abajo está verificado **contra las fichas** de `raw/Material_Catedra/bibliografia/` y,
donde se cita texto, contra el PDF. `—` = verificado que no está. El mapeo fino de la semana lo hace
[[_index-bibliografia]] › Clases 12–14 y Práctica 15/09.

| Tema | *Seven Databases* 2ª ed. *(cap. 4 MongoDB, impresas 93–133 · PDF 106–146)* | Corbellini et al. 2017 | GMUW *(contraste relacional)* |
| --- | --- | --- | --- |
| Índice `_id` automático, `getIndexes()` y el *script* del slide 28 | cap. 4 › Day 2 › § *Indexing…*, **impresa 111** *(PDF 124)*: *"Mongo automatically creates an index by the _id"*; salida de `getIndexes()` en la **113** | § 6, **p. 14**: *"document databases index elements by the document identifier"* | **8.3.1** *Motivation for Indexes* *(350)* |
| `ensureIndex(fields, options)`, `unique`, experimento de `phones` | **impresas 110–112** *(PDF 123–125)*: `populatePhones`, `explain("executionStats")`, 52 → 0 ms, *"scanned objects dropped from 109999 to 1"* | — | **8.3.2** *Declaring Indexes* *(351)* — sintaxis genérica, *"no es parte del estándar"* |
| Índice sobre campo anidado, `background` | **impresa 113** *(PDF 126)*: `ensureIndex({ "components.area": 1 }, { background : 1 })` | — | — |
| Costo de los índices, *"cost more in Mongo than … Postgres"* | **impresa 114** *(PDF 127)*, párrafo de cierre | § 6, **p. 14**: *"for each type of query a new index needs to be created"* | **8.4** *Selection of Indexes* *(352–359)* — el modelo de costo relacional |
| B-tree como estructura | **impresa 110**: *"the classic B-tree…"*; teoría, cap. 2 § *Fast Lookups with Indexing* *(18–20, TP5)* | — | **14.2** *B-Trees* *(633)* |
| `explain()`, verbosidades, `COLLSCAN`/`IXSCAN` | **impresas 111–112**: solo `"executionStats"`; **los nombres de las etapas no aparecen** | — | — |
| **Compuestos y multikey** *(TP9 paso 33)* | **—** *(el cap. 4 no crea ninguno)* | — | **14.6.1** *Multiple-Key Indexes* *(675)* — otra cosa |
| `dropIndex`, `dropIndexes`, `hideIndex` | **—** | — | — |
| Texto, TTL, hashed, parciales | **—** *(geoespacial sí: Day 3, 130–131)* | — | — |
| Índice `2d` y el Do.1 de Londres *(TP9 Parte II, ej. 5)* | cap. 4 › Day 3 › § *GeoSpatial Queries*, **impresa 130** *(PDF 143)*: *"The 2d index must be set on any two value fields"* y `ensureIndex({ location : "2d" })`; *Day 3 Homework*, Do.1, **impresa 132** *(PDF 145)* | — | **14.4** *Multidimensional Indexes* · **14.5** *Hash Structures for Multidimensional Data* *(grid files)* · **14.6** *Tree Structures for Multidimensional Data* *(kd-trees, quad trees, R-trees 14.6.7)* *(661–688)* — la teoría de índices espaciales, sin MongoDB |
| *Profiler* | **impresas 112–113** — el deck lo omite | — | — |

> [!warning] Los `4.N` no se usan, a propósito
> La ficha de *Seven Databases* advierte que los `4.1`–`4.5` de su tabla son extrapolación, no citas
> existentes; por eso las citas van por **título de sección y página impresa**.

**Lectura mínima sugerida** *(propuesta; la fija la etapa de bibliografía)*: *Seven Databases* cap. 4
› Day 2 › § *Indexing: When Fast Isn't Fast Enough* completo *(impresas 110–114, cinco páginas)* +
**Corbellini § 6, p. 14** *(dos párrafos)*. Para compuestos y multikey **no hay libro en el vault**:
la fuente es la documentación oficial, `manual/core/index-single/` *(el link del slide 29)* y sus
páginas hermanas `index-compound/` y `index-multikey/`.

## Dudas abiertas

- [x] ~~(crítico) ¿`ensureIndex` corre en el `mongosh` de la imagen `mongo` actual? Misma duda en
  [[Práctica 2026-09-15|Práctica 2026-09-15]] y [[Clase 14 - MongoDB Features|Clase 14 - MongoDB Features]].~~ (ok) **Sí**: en MongoDB 8.3.11 /
  `mongosh` 2.11.1 corre sin ninguna advertencia y devuelve `[ 'name_1' ]`; `dropDups` se acepta y
  no hace nada → [[Práctica 2026-09-22|Práctica 2026-09-22]] § *Qué del TP está deprecado* y § 2.2 de esta página.
- [ ] (crítico) ¿Qué versión de MongoDB baja `docker pull mongo` hoy? *(`db.version()` en la
  primera sesión.)* (nota) Cota de referencia: la imagen `mongo:8` resuelve hoy a MongoDB **8.3.11**
  con `mongosh` **2.11.1** ([[Práctica 2026-09-22|Práctica 2026-09-22]] § *Fuente y contexto*); sigue sin confirmarse
  qué trae un `docker pull mongo` sin tag en la fecha del TP.
- [ ] Paso 34 del TP9: ¿el planificador elige `name_1` o `name_1_weight_1` cuando existen los dos?
- [ ] ¿Entran índices en el parcial del 13/10 más allá de "crear, borrar, `explain`"?
- [x] ~~¿Se dicta algo de índices geoespaciales? *(el dataset `hospitales` del slide 33 de
  la Clase 14 es GeoJSON)*~~ (ok) **Sí**: el TP9 Parte II, ejercicio 5, pide el índice
  `2d` sobre `cities` y una consulta por radio → § 3.6 y [[Práctica 2026-09-22|Práctica 2026-09-22]] § Ejercicio 5.
- [ ] ¿Se dictan índices de texto? Ni el TP9 (Partes I y II) ni los decks 12–14 los usan.
- [ ] ¿La cátedra espera `sparse` / `partialFilterExpression` para documentos sin el campo *(caso
  Kane sin `height`)*?
- [ ] ¿Se acepta `ensureIndex` en la entrega, o hay que escribir `createIndex`? Pregunta ya anotada
  en [[Práctica 2026-09-15]] § *Preguntas para el docente*.
- [ ] Índice clustered en MongoDB *(5.3+)*: ningún material del vault lo menciona; se deja fuera
  hasta que aparezca.

## En los exámenes

| Examen | Pregunta | Qué se pregunta | Respuesta en una línea |
| --- | --- | --- | --- |
| [[Parcial 2Q2025\|Parcial 2Q2025]] | 9 *(opción múltiple)* | tipo de índice por defecto, si hay índices automáticos como en MySQL y sobre qué campo | **A — B-Tree · Sí · `_id`**; `getIndexes()` real sobre una colección nueva devuelve solo `_id_` |
| [[Parcial 23-5-23 - Bases de Datos Avanzadas\|Parcial 23-5-23 (BDA)]] *(temario anterior)* | 32 *(opción múltiple)* | la misma pregunta, contra PostgreSQL en vez de MySQL | **A) B-Tree – Sí – `_id`**; la consulta por `_id` da `EXPRESS_IXSCAN` sobre `_id_` |
| [[Parcial 23-5-23 - Bases de Datos Avanzadas\|Parcial 23-5-23 (BDA)]] *(temario anterior)* | 22 *(completar)* | qué motor provee `_id`, `_id` + `_rev` e `id` | MongoDB · CouchDB · Neo4j; en MongoDB el `_id` trae su índice `_id_` *(CouchDB fuera del temario 2026; Neo4j no dictado aún)* |
| [[Parcial 23-5-23 - Bases de Datos Avanzadas\|Parcial 23-5-23 (BDA)]] *(temario anterior)* | 28 *(V/F)* | ¿PostgreSQL + PostGIS brindan las mismas funciones espaciales que MongoDB? | **Falso**: MongoDB se limita a `2d`/`2dsphere` y unos pocos operadores; PostGIS *(fuera del temario 2026)* implementa mucho más |
| [[Parcial 1Q2021\|Parcial 1Q2021]] *(temario anterior)* | 5 *(práctico, motor libre)* | distancia recorrida por cada vehículo a partir de puntos GPS | resuelta por el vault en **MySQL** con `LAG()` + `ST_Distance_Sphere`; sin respuesta en la fuente |
| [[Parcial 1Q2021\|Parcial 1Q2021]] *(temario anterior)* | 6 *(práctico)* | vehículos a menos de 50 m de un punto WKT | MySQL: `MIN(ST_Distance_Sphere(…)) < 50`; sin respuesta en la fuente |
| [[Parcial 2Q2021\|Parcial 2Q2021]] *(temario anterior)* | 9 *(práctico)* | la misma distancia, en "el motor recomendado para datos geográficos" | resuelta por el vault en MongoDB: `$setWindowFields` con `$shift` + Haversine; coincide con el cálculo en MySQL |
| [[Parcial 2Q2021\|Parcial 2Q2021]] *(temario anterior)* | 10 *(práctico)* | vehículos a menos de 100 m de un punto WKT, en MongoDB | `$geoNear` como primera etapa, con índice `2dsphere` y `maxDistance: 100`; sin respuesta en la fuente |

Lo que más se pregunta es el **índice por defecto** (2023 y 2025, casi textual): B-tree, automático, sobre `_id`; la trampa es marcar "B-Tree · No", como el alumno de la captura del 2Q2025.
Lo geoespacial solo aparece en el temario anterior; ahí la trampa es el orden `[longitud, latitud]`.
(atención) La página del [[Parcial 2Q2021|Parcial 2Q2021]] atribuye al TP9 Parte II el índice `2dsphere`; el TP usa `2d` → § 3.6.

## Enlaces

- Clases: [[Clase 14 - MongoDB Features]] slides 28–29 *(el bloque completo; el 30 es la tabla CLI)* ·
  [[Clase 13 - NoSQL-EmbebidosVSNormalizado]] slide 28 *(`explain("executionStats")`, último slide)* ·
  [[Clase 12 - Introduccion a NoSQL]] slide 29 *("Soporte de índices", sin desarrollo)* ·
  [[Clase 11 - Seguridad-Transacciones]] slides 31–38 *(la teoría de estructuras: B-tree vs. hash,
  con y sin agrupación — es la que aquí se da por leída)*
- Práctica: **[[Práctica 2026-09-15]]** *(TP9 Parte I, p. 6, pasos 30–34)* · el método de medir:
  [[Práctica 2026-08-18|Práctica 2026-08-18]] *(TP5 Explain Plan, en MySQL)* · **[[Práctica 2026-09-22|Práctica 2026-09-22]]** *(TP9 Parte
  II: ejercicio 2, índice por defecto y `_id_` automático; ejercicio 3, `explain()` contra `EXPLAIN`
  de MySQL y `EXPRESS_IXSCAN`; ejercicio 5, índice `2d` sobre `cities`)*
- Exámenes: [[Parcial 2Q2025|Parcial 2Q2025]] *(pregunta 9)* · [[Parcial 23-5-23 - Bases de Datos Avanzadas|Parcial 23-5-23 - Bases de Datos Avanzadas]]
  *(preguntas 22, 28 y 32)* · [[Parcial 1Q2021|Parcial 1Q2021]] y [[Parcial 2Q2021|Parcial 2Q2021]] *(GPS y proximidad)*
- Conceptos hermanos de la Clase 14: [[2.14.01 - mongosh y herramientas de línea de comando|mongosh y herramientas]]
  *(el shell donde se escribe todo esto; `mongostat`/`mongotop`)* ·
  [[2.14.03 - MapReduce|MapReduce]] *(la otra forma de "acelerar": procesar en vez de buscar)*
- Conceptos de la U2 con los que se lee: [[2.12.06 - Modelo de documentos — JSON, BSON y ObjectId|Modelo de documentos]]
  *(`_id` y `ObjectId`; por qué "rows" está mal dicho)* ·
  [[2.12.07 - CRUD y consultas en MongoDB|CRUD y consultas]] *(los selectores que un índice acelera;
  `$regex` anclado)* · [[2.12.08 - Aggregation pipeline|Aggregation pipeline]] *(`$match` al inicio
  del pipeline usa índices; `$lookup` usa el de la colección foránea)* ·
  [[2.13.01 - Documentos embebidos vs. referencias|Documentos embebidos vs. referencias]] *(lo
  embebido no necesita índice aparte; la unicidad como único constraint)* ·
  [[2.12.02 - Escalabilidad horizontal — sharding y replicación|Sharding y replicación]] *(la shard
  key lleva índice; índices hashed)*
- Contraste con la U1: [[1.08.02 - Índices|Índices]] *(la teoría y las cinco reglas que se
  transfieren enteras)* · [[1.08.01 - Plan de ejecución|Plan de ejecución]] § *`explain()` en MongoDB*
  *(el detalle del plan)* · [[1.09.01 - Restricciones de integridad|Restricciones de integridad]]
  *(lo que MongoDB no tiene: solo el índice único)*
- Motores: [[MongoDB]] *(§ Versión — qué baja `docker pull mongo`; § 3 · Índices de la tabla slide vs. `mongosh`)* ·
  [[MySQL]] *(`CREATE INDEX` / `DROP INDEX … ON`, `USING HASH` ignorado)*
- Bibliografía: [[_index-bibliografia]] › Clases 12–14 y Práctica 15/09 — ★★ *Seven Databases* cap. 4
  › Day 2 › § *Indexing: When Fast Isn't Fast Enough* *(impresas 110–114)* · **Corbellini § 6, p. 14**
  · contraste: **GMUW 8.3** *(Indexes in SQL, 350–352)* y **14.2** *(B-Trees, 633)* · manual de
  MongoDB `core/index-single/` *(link del slide 29)*
