# Decisiones de arquitectura (N0)

Registro numerado de las decisiones tomadas por el orquestador durante la ejecución autónoma
del Sprint 1. El usuario pidió "cualquier decisión dejarla para el final": aquí están todas,
con su porqué, para revisarlas en bloque. Cada una se puede revertir; se anota el costo.

| # | Decisión | Por qué | Costo de revertir |
|---|---|---|---|
| N0-1 | **Monorepo pnpm** con `apps/web` (SPA), `apps/api` (servidor), `packages/contract`, `packages/markdown`, `packages/cli`. | Un solo `pnpm install`, tipos compartidos sin publicar paquetes, deploy de API + estáticos desde un repo. | Bajo. |
| N0-2 | **SQLite vía `@libsql/client` + Drizzle ORM**, archivo local `apps/api/data/sinapsis.db`. | Volumen chico (miles de páginas markdown), cero operación local, y el mismo cliente apunta a Turso (libSQL en la nube) cambiando solo la URL cuando haya deploy. FTS5 disponible para búsqueda. | Medio si se quisiera Postgres: Drizzle abstrae el esquema pero FTS5 y algunos tipos cambian. |
| N0-3 | **API en Hono** (`@hono/node-server`) en vez de Express/Fastify. | Tipado, liviano, corre en Node hoy y en Bun/edge mañana; middlewares de cookies y zod integrados. | Bajo: rutas finas, lógica en servicios. |
| N0-4 | **Autenticación: Google Identity Services en el cliente → ID token → verificación en el servidor (`google-auth-library`) → cookie de sesión httpOnly firmada**, sesiones en tabla `sessions`. | Sin proveedor externo de sesión, funciona en localhost y en deploy. | Bajo. |
| N0-5 | **Bypass de desarrollo**: con `AUTH_DEV_BYPASS=1` y `NODE_ENV!=production`, `POST /api/auth/dev` inicia sesión como usuario local. | El Client ID de Google requiere una acción del usuario en Google Cloud Console que el agente no puede hacer; el MVP debe poder probarse de punta a punta sin ese paso. El bypass está prohibido en producción por código. | Ninguno: se elimina la variable. |
| N0-6 | **Materias globales, landing por usuario.** `subjects` es global (la plataforma es personal; el CLI sincroniza sin usuario); `user_subjects` guarda cuatrimestre y posición por usuario; `progress` es por usuario. | Es lo que hace el mockup: el usuario ordena SU landing; el sync viene de un CLI local. Evita multi-tenant prematuro. | Medio: agregar `owner_id` a `subjects` cuando haya más de un usuario real. |
| N0-7 | **Token de sync estático** (`SYNC_TOKEN` en `.env` del API) para `PUT /api/subjects/:slug/sync`. | El CLI corre en la misma máquina; tokens por usuario son Sprint 2+. | Bajo. |
| N0-8 | **Tema global por usuario** (`users.theme`), no por materia. Resuelve la "decisión pendiente" del mockup. | Una preferencia de lectura es de la persona, no del contenido; evita saltos de piel al cambiar de materia. Se persiste además en localStorage para pintar antes del primer paint. | Bajo: agregar override por materia después. |
| N0-9 | **Rutas de navegador** (`/m/:subject/p/:slug`), no hash. | SPA con servidor propio; URLs limpias y compartibles; el API sirve `index.html` como fallback. | Bajo. |
| N0-10 | **Markdown en el cliente con `react-markdown` + `remark-gfm` + `remark-math` + `rehype-katex`** y plugins propios para wikilinks `[[slug|texto]]` y callouts de Obsidian `> [!tipo]`. El cuerpo viaja crudo desde la API. | Mismo pipeline que Obsidian; el servidor no renderiza HTML (menos superficie XSS); KaTeX ya vendorizado en el baseline. | Medio si se quisiera SSR. |
| N0-11 | **Rail: grupos FIJOS los dibuja la plataforma; los grupos SLOT vienen del `sinapsis.config.json`.** Ítems con `kind` = `builtin` / `page` / `link` / `tool`. En el Sprint 1 `tool` muestra "Próximamente". | Es el contrato 00 del mockup. Las herramientas React propias por materia (plugins) son Sprint 3. | Bajo. |
| N0-12 | **Escala de color de división paramétrica** (N ≤ 9: `--u1…--u9`; N > 9: barrido oklch), implementada en `@sinapsis/contract` y compartida por web y CLI. | Decisión visible en el mockup 00; una sola implementación. | Bajo. |
| N0-13 | **El compilador del wiki es un paquete TypeScript (`@sinapsis/markdown`)** que reimplementa el contrato de `build.py` (frontmatter, wikilinks, headings, orden). | Un solo lenguaje en el repo; el CLI y los tests lo usan directamente; `build.py` queda como referencia. | Bajo. |
| N0-14 | **Primera materia real: Probabilidad y Estadística**, con `sinapsis.config.json` escrito por el orquestador a partir del HANDOFF (unidades 1-9, 0, eval; tipos concepto/distribución/teorema/técnica/formulario/fuente). El config vive en `examples/proba/` del repo y se copia al vault cuando el usuario lo apruebe. | Valida el sync con 200 páginas reales sin tocar el vault del usuario. | Ninguno. |
| N0-15 | **Estilos: CSS plano con los tokens del design system EstudioDS** (los tres temas, tipografías vendorizadas) + CSS Modules por componente. Sin Tailwind. | El mockup y el baseline son token-first; Tailwind duplicaría la paleta. | Alto (reescritura visual). |
| N0-16 | **Estado del cliente: TanStack Query para datos del servidor, Zustand para UI (tema, panel plegado, divisiones abiertas).** | Estándar, chico, testeable. | Bajo. |
| N0-17 | **Vite proxy `/api` → `localhost:3000` en desarrollo; en producción el API sirve `apps/web/dist`.** Un solo origen: sin CORS, cookies `SameSite=Lax`. | Simplicidad y seguridad. | Bajo. |
| N0-18 | **Alcance del Sprint 1 (MVP)**: landing completa (grilla por cuatrimestre, agregar/quitar/mover/reordenar, vacía, temas, login), shell de materia (rail FIJO+SLOT, índice adaptable, cabecera, migas, Inicio con progreso, Lector con KaTeX/wikilinks/TOC/fuentes/backlinks/marcar estudiado, catálogo "Todo el wiki" con búsqueda y filtros, índice plegado), API + DB + sync, CLI, skill `/sinapsis` (init/sync/status). **Fuera**: pestañas múltiples, grafo, flashcards/quiz, plan/kits, figuras interactivas, herramientas React por materia, tokens por usuario, deploy. | Es el corte que deja la plataforma usable con una materia real y el contrato probado. Lo demás son sprints 2-4. | — |
| N0-19 | **Modelos de trabajo**: orquestación en la sesión (Fable) para contratos, integración y adjudicación final; agentes de construcción y verificación de ola en Opus 5. | Instrucción del usuario del 2026-09-05. | — |
| N0-20 | **La hoja de lectura mide 840 también con el índice abierto a 1440**: se recortan los márgenes laterales del lector (16 px) en vez de estrechar la columna de 248 o bajar la cota del artboard 00. | El contrato declara 840 como medida fija; el propio mockup dibujaba 840 dentro de una ranura de 808. | Bajo. |
| N0-21 | **El H1 duplicado se corta en el compilador**, no en el lector: cuando el título de la página proviene del primer `# H1` del cuerpo (o coincide con él), `Page.body` se emite sin esa línea y `headings` no la incluye. | Una sola decisión guardada una vez; el FTS deja de indexar el título dos veces; cualquier consumidor futuro lo hereda. Es la única divergencia deliberada con `build.py`. | Bajo (re-sync). |
| N0-22 | **El compilador es el dueño de los ids de encabezado** (`headingId` en el contrato, algoritmo de `build.py`); el lector aplica el mismo algoritmo al renderizar en vez de `rehype-slug`. | `[[pagina#ancla]]` lo escribe el autor del wiki contra el criterio del compilador; dos algoritmos obligaban a leer los ids del DOM y a buscar sin acentos. | Bajo. |
| N0-23 | **Las divisiones sintéticas «Transversales» (páginas sin división) y «Otras» (división no declarada) las define el contrato** (`effectiveDivisions`, `divisionOf`) y no el cliente. No cuentan como divisiones declaradas en la tarjeta de la landing. | Una misma pregunta («¿en qué división cae esta página?») se respondía distinto en API, web y compilador. | Bajo. |
| N0-24 | **Modo mock del frontend con una sola costura** (`installMockApi` en `lib/api.ts`, activado en el arranque solo en desarrollo con `?mock=1`); ninguna feature vuelve a mencionar el mock. | Las ramas `isMockMode()` repartidas dejaban fixtures en el bundle de producción. | Bajo. |
| N0-25 | **Tokens de contraste**: los rótulos versalita usan las tintas `--warn-text` / `--accent-ink` (no `--warn` / `--accent`), el anillo de foco es `--primary` opaco, y los controles deshabilitados bajan solo a 0.75. | Mediciones WCAG AA sobre los tres temas en la auditoría UX. | Ninguno. |
| N0-26 | **Alcance del Sprint 2 («Estudio»)**: pestañas múltiples, grafo de conexiones, favoritos y apuntes, flashcards con SRS (SM-2), quiz, plan de estudio y kits; más los diferidos S-03 (cuatrimestres persistentes), S-06 (FTS por materia) y S-07 (tabla de enlaces). Fuera: herramientas React por materia (Sprint 3), deploy (Sprint 4), figuras interactivas. | Es el bloque «Estudio» de `docs/SPRINTS.md`; los diferidos entran porque el grafo y la escala a varias materias los necesitan. | — |
| N0-27 | **El material de estudio viene del wiki**, en la carpeta `wiki.study` (por defecto `estudio/`): mazos y quizzes en markdown (`tipo: flashcards` / `tipo: quiz`, una tarjeta o pregunta por `##`), plan y kits en JSON validado por zod (`plan.json`, `kits.json`). Se compila y viaja en `SyncPayload.study`. Si la materia no trae mazos, la plataforma autogenera uno por división con título → resumen de cada página (`autoDecks`, marcado `auto`). | El contenido de estudio es contenido de la materia, no de la plataforma (misma regla que las páginas); el mazo automático da valor inmediato a cualquier wiki con `resumen`. | Medio (formato de archivos). |
| N0-28 | **SRS = SM-2 con cuatro notas** (otra vez · difícil · bien · fácil), implementado como función pura en el contrato (`sm2`) y persistido por usuario y materia en el API; la web solo previsualiza el próximo intervalo. | Es el algoritmo del baseline y el estándar de Anki; una sola implementación evita que el cliente y el servidor calculen distinto. | Bajo. |
| N0-29 | **Pestañas múltiples solo en el cliente** (localStorage por materia, hasta 20, reordenables, ⌘-clic abre en nueva pestaña, cada una recuerda su scroll), sin persistencia en el servidor. | Es estado de sesión de lectura, no de progreso; el baseline lo hacía igual. | Bajo. |
| N0-30 | **Cierre de S-08 y S-09**: el segundo botón del Inicio queda como «Todo el wiki» hasta que existan herramientas de materia (Sprint 3) y la landing conserva el filtro en línea («Filtrar materias…») sin paleta; la paleta ⌘K es exclusiva del shell de materia. | Evita prometer una paleta global que no existe y no depende de herramientas aún inexistentes. | Bajo. |
| N0-31 | **Grafo de conexiones con `d3-force` sobre canvas**, alimentado por `GET /api/subjects/:slug/graph` (tabla `page_links` poblada en el sync, S-07), con filtros por división y tipo y clic → lector. | Layout probado y liviano; la tabla de enlaces también indexa los backlinks. | Bajo. |
| N0-32 | **Cuatrimestres del usuario persistidos** (`user_semesters`): la landing puede tener cuatrimestres vacíos y ordenarlos; `LandingLayoutInput.semesters` los declara; quitar la última materia de un cuatrimestre no lo elimina; los rótulos se guardan en forma canónica («2026-1C»). Cierra S-03. | Era el único estado de la landing que se perdía al guardar. | Bajo. |
| N0-33 | **Nombres de sección del estudio**: «Flashcards», «Quiz» y «Kits de estudio» se conservan como nombres propios del producto (son los del baseline y de uso corriente en español); los objetos internos van en español (mazo, tarjeta, pregunta, hito, tarea). | Evita inventar traducciones que nadie usa («tarjetas de memoria»); la auditoría UX pedía fijarlo. | Bajo. |
| N0-34 | **Pestañas**: se completa el patrón ARIA de *tabs* (`tabpanel` + `aria-controls`, Home/End) en vez de degradar a una lista con `aria-current`; «Nueva pestaña» abre «Inicio» y los títulos repetidos se numeran («Inicio 2»); el cierre de pestaña por teclado pasa de ⌘W (reservado por el navegador) a ⌘⇧W. | Es el patrón que ya se implementó y el que el baseline sugería; ⌘W no se puede interceptar en Chrome. | Bajo. |
| N0-35 | **Apuntes**: el contrato de guardado es autoguardado (800 ms + al perder el foco) con un botón explícito «Guardar apunte» (⌘S) como refuerzo; un guardado fallido conserva el texto y muestra «No se pudo guardar» con reintento; borrar pide confirmación. | Los tres caminos convivían sin definirse y un error vaciaba el textarea (bug 1 de la auditoría). | Bajo. |
| N0-36 | **Columna lateral del lector bajo 1280 px**: panel deslizante (botón «PANEL») en vez de desaparecer, para que apuntes, índice, fuentes y backlinks sigan accesibles. | Bloqueante UX del Sprint 2. | Bajo. |
| N0-37 | **Plan de estudio**: se mantiene la simplificación respecto del baseline (sin conmutador de modalidad ni chips por instancia) hasta que el contrato tenga `Plan.tracks` (S-11, Sprint 3); las fechas por fase ya existen en el contrato y se muestran cuando el JSON las trae. | El contrato actual no modela modalidades; agregarlas a la vista sin dato detrás sería decorativo. | Bajo. |
| N0-38 | **Borrador de gestión de la landing**: sigue siendo implícito, pero ya no se descarta en silencio: ⌘K no lo cierra, y salir de gestión con cambios pide confirmación. «Quitar cuatrimestre» solo aparece si está vacío y el botón deshabilitado explica por qué cuando tiene materias. | Bloqueante UX (⌘K borraba el borrador) y consistencia con «Quitar materia», que sí confirma. | Bajo. |
| N0-39 | **Grafo en canvas**: se mantiene (decisión N0-31); la accesibilidad por nodo se resuelve con foco en el lienzo + flechas para recorrer nodos y la lista «Más citadas» ampliada a 30 con «ver más». | Una capa SVG paralela de 209 nodos duplicaría el trabajo de render. | Medio si se quisiera SVG. |
| N0-40 | **Alcance del Sprint 3 («Herramientas»)**: sistema de plugins por materia (`kind: "tool"`), figuras interactivas del wiki (`[!figura]`), `Plan.tracks` (S-11), `dueCount` en la landing, contrato de **propuestas de cambio a la plataforma** (rama + PR + revisión del orquestador), consolidación de todos los contratos en `docs/contracts/`, y al cierre una revisión de diseño con el navegador más una comparación página a página con la app original de Proba. | Pedido del usuario del 2026-09-06. | — |
| N0-41 | **Las herramientas de una materia son bundles de scripts clásicos** (IIFE) que se cargan contra un runtime de compatibilidad `window.App` / `window.M` con la misma superficie que el baseline de Proba (`registerView`, `registerFigure`, `Fig`, `Plot`, `renderMarkdown`, `go`…), más `window.SinapsisRuntime`. El bundle lo construye y sube el CLI (`sinapsis tools build/push`) y lo sirve el API desde `/api/subjects/:slug/tools/:id/files/*`. | Permite reutilizar casi verbatim las ~14 000 líneas de herramientas y figuras de Proba (explorador, calculadoras, taller, laboratorio, buscador de valores, 92 figuras) en vez de reescribirlas en React; una materia nueva puede escribir sus herramientas en JS plano o en React usando el runtime. El código corre en el origen de la plataforma: es aceptable porque los repos de materia son del propio usuario (personal), y queda anotado para revisar si la plataforma se comparte (S-14: sandbox por iframe). | Alto si se quisiera un modelo de plugins React puro. |
| N0-42 | **Las figuras interactivas también son bundles** (`figures: true` en el manifiesto): el lector monta `[!figura] id` con `App.mountFigures` cuando la materia tiene un bundle de figuras cargado; sin bundle, se muestra el epígrafe como hasta ahora. | Misma pieza que las herramientas; el motor (`figures.js`, `plot.js`, `lib-math.js`) se porta al runtime de la plataforma para que cualquier materia lo use. | Bajo. |
| N0-43 | **`Plan.tracks`**: modalidades alternativas del plan (cursada vs. final directo) con sus propias fases; `phases` sigue siendo la modalidad por defecto; los ids de tarea son globales. La vista muestra un conmutador y recuerda la elección por materia en el cliente. **Ampliación (propuesta de proba, 2026-09-06)**: una fase con el mismo id en dos modalidades es la misma fase (contenido idéntico, sus tareas no cuentan como repetidas); documentado en el tipo y probado en el contrato. | Cierra S-11 sin romper los planes existentes. | Bajo. |
| N0-44 | **Propuestas de cambio a la plataforma desde una materia**: el agente de materia trabaja en una rama `proposal/<materia>-<slug>` del repo de Sinapsis, escribe `proposals/<fecha>-<slug>.md` (motivo, alcance, archivos, gates), commitea y, si hay remoto GitHub y `gh`, abre un PR; si no, deja la rama y una entrada en `proposals/INBOX.md`. El orquestador (sesión en el repo de la plataforma, skill `/sinapsis-review`) revisa de forma adversarial, corre los gates, decide, mergea a `main` con `--no-ff` o rechaza con motivos escritos en la propuesta, y registra la decisión. Nunca se mergea sin revisión. | Es el flujo «empresa con sectores incomunicados» que pidió el usuario: la materia propone y ejecuta, la plataforma decide. | Bajo. |
| N0-45 | **Contratos consolidados** en `docs/contracts/` (materia, páginas, estudio, herramientas y figuras, sync y API, skill, propuestas) con `docs/CONTRACT.md` como índice; la fuente ejecutable sigue siendo `packages/contract`. | Un solo lugar por contrato, legible por los agentes de materia. | Ninguno. |
| N0-46 | **Fidelidad con la app original de Proba**: el criterio de cierre del Sprint 3 incluye una comparación visual página a página (inicio, lector, catálogo, plan, explorador, calculadoras, figuras) en los tres temas; las diferencias se clasifican en «intencionales por contrato» (se documentan) o «defectos» (se corrigen). | Pedido del usuario. | — |
| N0-47 | **Normalización de `$$` en el compilador**: `normalizeDisplayMath` pone los delimitadores de display en líneas propias (`$$ f(x)` → `$$` / `f(x)`; `g(x) $$` → `g(x)` / `$$`; `$$ h $$` solo en su línea → bloque de tres líneas). Los encabezados se extraen del cuerpo normalizado, lo que recupera los que `build.py` omitía del índice tras un cierre `g(x) $$`. | remark-math trata `$$ f(x)` como cerca con «meta» (la fórmula se pierde y el bloque se traga el resto de la página: 51 páginas de Proba truncadas) y renderiza `$$ h $$` en una línea como inline; el baseline empareja `$$…$$` sin importar los saltos. | Bajo. |
| N0-48 | **Ampliación de `CompatApp` (S-16)**: `App.$`/`App.$$` acotados al contenedor de la vista viva, `App.paletteOpen()` como PREGUNTA (¿está abierta la paleta?) y `App.openPalette()` como acción, y `SinapsisRuntime.bindView(contenedor)` que delega los clics de `[data-nav]`, `[data-go]` y `a.wikilink[data-slug]` hacia `App.go` y limpia `setRedraw` al desatar. `App.quickLookup` queda fuera del contrato (lo instala el bundle de Proba). | `lookup.js` del baseline usa `paletteOpen()` como predicado dentro de su guardia de Escape: implementarla como acción haría que Escape abriera la paleta. La delegación en el runtime evita listeners globales en `document` y se prueba en el paquete. | Bajo. |
| N0-49 | **Cierre del Sprint 3 con revisión visual y auditoría en dos lentes**: la hoja de KaTeX la carga el runtime desde `figures.css` (F5-1) aunque el lector ya la traiga (~9 kB gzip duplicados) para que las herramientas y el inicio no muestren la fórmula dos veces al entrar por URL; las capturas masivas de las revisiones (538 archivos, 180 MB) quedan fuera del repo salvo las 47 que citan `docs/DISENO-sprint3.md` y `docs/FIDELIDAD-sprint3.md`; la auditoría se hizo con dos auditores (seguridad y corrección) de solo lectura y el orquestador adjudicó leyendo el código y reproduciendo. | Un `import` global de KaTeX en la web cargaría la hoja en la landing, que no la usa; versionar 180 MB de PNG haría inusable el repo; el patrón de auditoría es el de los Sprints 1 y 2. | Bajo. |
| N0-50 | **Tarjeta de vista previa de página, general y del shell** (`PageTip`, port de `tips.js` del baseline): UNA sola tarjeta montada en `SubjectShell` que por delegación cubre todo enlace a una página —wikilinks, índice, segmentos de la barra de unidad, backlinks, fuentes, catálogo, kits, herramientas— con título, división y tipo, resumen (`Page.summary`; sin él, el primer párrafo del cuerpo), la sección apuntada por el ancla y «N palabras · leída ✓»; en la barra de unidad agrega «4 de 12 · sin leer», que reemplaza al tooltip mini del baseline. Los enlaces a páginas pierden su `title` nativo. | El baseline lo tenía y era la forma de decidir si vale la pena seguir un enlace sin abrirlo; hacerlo general (y no del lector) evita que cada vista invente el suyo, y ata el valor del campo `resumen` del wiki a algo visible. | Bajo: se desmonta el componente del shell y vuelven los `title`. |
| N0-51 | **Ajustes del lector y del rail pedidos por el usuario (2026-09-06)**: sin botón «Atrás» en la cabecera (el historial es del navegador; las migas dan el contexto); el conmutador del índice arriba del rail, bajo el sello, con el icono de panel lateral; el rail lleva un rótulo vertical por grupo en el borde izquierdo y la barra del ítem activo sigue pegada al borde, con el tinte extendido hasta ella; las acciones «Marcar estudiado / A favoritos» viven en la línea de identidad de la hoja (se quitan «¿Qué sigue?» y «+N fuentes»); «← Anterior / Siguiente →» flanquean la barra de unidad; sin rótulo de tipo sobre la barra; la barra conserva la paleta por estado y el tooltip lleva etiqueta de tipo e insignia de lectura; la columna derecha solo trae el índice de la página y los apuntes (fuera «Fuentes» y «Enlazan aquí»); canaletas iguales a los dos lados de la hoja con el botón «PANEL» a la derecha de la columna; el índice de la página desplaza solo el contenedor de la materia. Proba: sin el enlace al campus en el rail y con el formulario primero. | Revisión visual del usuario sobre la página de Proba. | Bajo. |
| N0-52 | **Fechas del plan por instancia evaluatoria**: `Plan.instances` (parcialitos, parcial, recuperatorio, final) y `PlanPhase.instance`/`retake`; la fecha la carga cada usuario y vive en su cuenta (`StudyState.planDates`, tabla `plan_dates`, `PUT/DELETE /study/plan-dates[/:key]`), nunca en el wiki; `PlanPhase.date` queda como fecha por defecto del cronograma. «Reiniciar el plan» (`DELETE /tasks`) no borra fechas. La vista del plan copia la forma del original (panel de fechas, cuenta regresiva, ritmo sugerido, navegación por fases, «Completar fase»). Los campos de fecha usan el `DatePicker` propio de la plataforma. | Pedido del usuario: «al plan le faltan las fechas y quedó muy distinto del original»; en el original las fechas vivían en localStorage, acá en la cuenta para que sigan al usuario. | Medio: migración 0004 y rutas nuevas. |
| N0-53 | **Los lanzadores de kits y las tareas `tool` pueden apuntar a vistas fijas de la plataforma** (flashcards, quiz, plan…) además de a ítems del rail de la materia; el compilador valida contra las dos listas. | Los kits del original lanzan «Flashcards» y «Quiz»; sin esto el sync los marcaba como referencias rotas. | Ninguno. |
| N0-54 | **Auditoría contra el original sin refutación**: por pedido del usuario, las 257 brechas encontradas por los 12 buscadores pasan directo a corrección (un fixer por vista con ownership disjunto), y las tres vistas no portadas (ejercicios, simulador de parcial, formularios) se portan como segundo bundle de Proba (`proba-exercises`). Las brechas que chocan con decisiones explícitas del usuario (sin «¿Qué sigue?» ni «+N fuentes», «Siguiente →» a secas, columna derecha con índice y apuntes) no se corrigen. | Velocidad sobre certeza: el usuario prefiere ver las correcciones y ajustar después. | Bajo. |
| N0-55 | **Ola de brechas cerrada (2026-09-06)**: de las 257 brechas encontradas contra la app original se corrigieron ~220 en once fixers (unidad, inicio, catálogo, grafo, apuntes/favoritos, kits, flashcards/quiz, armazón, herramientas/runtime, lector, y el port de ejercicios + simulador de parcial + formularios como bundle `proba-exercises`); el orquestador aplicó los pedidos cruzados (`Page.hub` en el API con migración 0005, migas de división/kit, `PlanTask.query` para los simulacros por unidades, `MathText` con display y negrita, carril de progreso más apagado). Los deslizadores pasan a ser un solo control de plataforma (`base.css` + `lib/rangeFill.ts`). ⌘-clic sobre un enlace abre una pestaña y la activa (como el original); el botón del medio la abre detrás (precisa N0-29). | Pedido del usuario: «revisa toda la página original para ver las cosas que no hiciste bien». | Medio: mucho código nuevo con tests (1 031 unitarios, 92 E2E). |
| N0-56 | **La plataforma deja de tener servidor: es un sitio estático en GitHub Pages** (`https://sebascaules.github.io/Sinapsis/`), sin API, sin base de datos y sin inicio de sesión con Google. `apps/api/` se elimina. Todo lo personal —perfil, tema, landing, páginas estudiadas, favoritos, apuntes, repaso espaciado, tareas del plan, fechas e intentos de quiz— vive en el navegador como un solo documento (`LocalBackup`): IndexedDB (`sinapsis`/`state`, clave `backup`) con espejo en `localStorage` (`sinapsis.backup`), gana la copia de `savedAt` más reciente, `navigator.storage.persist()` pedido en la primera mutación del usuario, `manifest.webmanifest` para instalarlo como aplicación, y copia de seguridad exportable e importable desde el menú del avatar. El cliente local implementa la misma interfaz `ApiClient` de `lib/api.ts`, así que ninguna vista cambia. | Pedido del usuario del 2026-09-06: «eliminar la autenticación y el API; que sea una página estática con todo embebido y que el progreso viva en el navegador, lo más persistente posible». Sin servidor no hay operación, ni costo, ni secretos, ni deploy que mantener; el uso es de una sola persona en su máquina, que es exactamente el caso que el modelo local cubre bien. | **Alto** si se quisiera multiusuario con servidor: volverían el API, la base, la sesión y la migración del estado del navegador a la cuenta. Para un solo usuario, ninguno: la copia de seguridad es el estado completo. |
| N0-57 | **Las materias son fuente dentro del repositorio de la plataforma**, en `subjects/<slug>/` (`sinapsis.config.json` con `wiki.root: "wiki"` y `wiki.study: "estudio"`, el wiki markdown, `estudio/` y los bundles de `tools/`), y se **compilan en el build** (`sinapsis site build` → `apps/web/public/subjects/**`, no versionado): `index.json` (catálogo global), y por materia `subject.json`, `pages.json`, `tools.json` y los archivos de cada bundle. La landing muestra **todo el catálogo**; ocultar, reordenar, agrupar por cuatrimestre y crear materias placeholder son decisiones locales de cada persona (`LocalLanding`). | Sin servidor, la fuente de una materia tiene que estar en algún lado versionado: el propio repositorio es el lugar obvio, hace el contenido revisable en un diff y deja el sitio reproducible desde cero. Compilar en el build (y no versionar el resultado) evita duplicar el contenido y que el JSON generado se desincronice del markdown. Mostrar todo el catálogo es lo que hace útil un sitio sin cuentas: quien entra ve las materias que hay, y su landing es una preferencia, no un permiso. | Bajo: el compilador y el formato ya existen; volver a un modelo de materias externas es cambiar de dónde lee `site build`. |
| N0-58 | **Una materia se integra por pull request.** `sinapsis publish` compila, valida y copia la materia a `subjects/<slug>/` en un worktree temporal creado desde `origin/main`, en la rama `subject/<slug>-<AAAAMMDD>`, commitea y abre el PR; el CI corre los gates y comprueba que el diff **no salga de `subjects/<slug>/`**; el orquestador lo adjudica con `/sinapsis-review` (mismo comando que las propuestas `proposal/*`, con criterios propios) y mergea con `gh pr merge --merge`, sin *squash*, para conservar el historial de la materia; el merge dispara el deploy. **Ningún commit del flujo lleva trailers de coautoría** (`Co-Authored-By`, `Claude-Session` o equivalentes), ni el del CLI, ni el de la revisión, ni el de merge. | Es el modelo de sectores incomunicados aplicado al contenido: la materia produce y propone, la plataforma revisa y decide, y cada cosa que entra queda con su diff y su veredicto. La regla de alcance evita que un PR de contenido cambie la plataforma de contrabando. La regla de coautoría es una instrucción global del usuario y pisa cualquier plantilla del entorno. | Bajo: es flujo de git, no código. |
| N0-59 | **El sitio vive bajo un subdirectorio.** Vite con `base: process.env.VITE_BASE ?? "/"` (el workflow de Pages exporta `VITE_BASE=/Sinapsis/`); el router usa `basename` = `import.meta.env.BASE_URL` sin la barra final; todo `fetch` de datos compone su ruta con `sitePaths.*(import.meta.env.BASE_URL, …)`; el build copia `dist/index.html` a `dist/404.html` y `public/.nojekyll` queda vacío. | GitHub Pages sirve el proyecto en `/Sinapsis/` y **no tiene fallback de SPA**: sin `404.html` toda ruta interna abierta directamente daría 404, y sin `basename` ninguna ruta resolvería. Componer las rutas desde `BASE_URL` en un solo lugar evita que cada vista adivine el prefijo. | Bajo: con dominio propio en la raíz, `VITE_BASE` vuelve a `/` y el resto sigue igual. |
| N0-60 | **La búsqueda y el grafo se calculan en el cliente.** La búsqueda arma un índice en memoria desde `pages.json` y conserva la **misma heurística de ranking** del servidor (`scoreHit` / `queryTerms` portados como funciones puras con sus tests: candidatos por substring sobre título, resumen y cuerpo normalizados con `fold`, menos peso para los tipos que no cuentan como contenido, snippet de unas 14 palabras alrededor de la primera coincidencia del cuerpo o, si no hay, el resumen). El grafo arma sus nodos desde `pages` (división efectiva con `divisionOf`) y sus aristas desde `links`. **FTS5 desaparece.** | Sin base de datos no hay motor de texto completo, y un wiki de un par de miles de páginas entra sobrado en memoria. Portar la heurística en vez de reinventarla mantiene los mismos resultados que el usuario ya conocía, y los tests que la cubrían siguen valiendo. | Medio si el corpus creciera mucho: habría que indexar (por ejemplo, un índice invertido pregenerado en el build), no volver al servidor. |
| N0-61 | **El progreso de una unidad suma los pasos que declaran los bundles.** Un bundle con `progress: true` en el manifiesto se carga al ENTRAR en la materia (como los de figuras) y registra proveedores con `App.registerProgressProvider({ id, label, stepsOf(division) })`; cada paso vale uno, igual que una página leída, y avisa de sus cambios con `App.progressChanged()`. En Proba, cada ejercicio de la unidad es un paso —los de la guía, los de Lutzio, los de parciales y los de finales— y está hecho cuando su estado es ≥ 1 (resuelto solo, con poca ayuda o con mucha). La barra y el porcentaje del hero de la división y las filas del inicio usan el total combinado; el texto lo desglosa («12 / 20 páginas leídas · 8 / 34 ejercicios resueltos») y la portada lista una tarjeta por grupo. El progreso del lector y el tooltip del índice siguen hablando solo de páginas. | Pedido del usuario del 2026-09-06: «quiero que en la barra de progreso de cada unidad se incluyan todos los ejercicios de esa unidad: los de la guía, los de Lutzio, los de parcial». La app original ya mostraba las colecciones de ejercicios como pasos de la barra de unidad pero NO las contaba; acá sí, porque es el trabajo real de la unidad. El mecanismo es genérico: cualquier materia puede sumar sus propios pasos sin tocar la plataforma. | Bajo: el modelo ya desglosa páginas y pasos (`progressParts`), y una materia que no declare `progress` queda exactamente como estaba. |
| N0-62 | **La hoja del lector se redimensiona a los costados.** Dos asas laterales (arrastre o teclado: flechas, Home/End, Enter vuelve a 840; doble clic también) cambian el ancho de la hoja de forma simétrica —está centrada en la columna, así el borde sigue al puntero— entre 620 y 1320 px y nunca más que el ancho disponible; la preferencia es global (no por materia) y vive en `localStorage` (`sinapsis.sheetWidth`). El 840 de N0-20 pasa a ser el valor por defecto, no una medida fija. | Pedido del usuario del 2026-09-06: «que el card de la página de la wiki sea agrandable para los costados manteniendo unos mins y maxs razonables». | Bajo: quitar las asas devuelve el 840 fijo. |
| N0-63 | **Columna derecha del lector pegada al borde y botón «PANEL» pegajoso.** Con la columna abierta, el botón vive en un dock pegajoso (`.sideDock`) al ras del borde izquierdo de la columna y se desplaza con ella; la columna queda a 8 px del borde derecho del área de lectura (ya no hace falta la canaleta de 36 px del botón). Cerrada, el botón vuelve a la canaleta derecha en su propio dock pegajoso (`.tabDock`). Bajo 1280 px se conserva el panel flotante. | Pedido del usuario del 2026-09-06: «el botón de panel debe mantenerse sticky al sidebar y al scroll y pegarse al borde izquierdo del sidebar cuando está en show» y «sácale el padding de la derecha al sidebar». | Bajo. |
| N0-64 | **Marco de página compartido.** La hoja, la cabecera de identidad (chip de división, etiqueta de tipo, posición, acciones) y la barra de la unidad con su Anterior/Siguiente dejan de ser del lector: son un componente de plataforma (`components/PageFrame`) que usan el lector del wiki **y** las vistas de herramienta que lo declaran en su manifiesto con `frame: "page"` (`ToolView.frame`). El marco se aplica cuando el `?arg=` de la vista resuelve a un grupo de progreso de una división (`model.stepForTool`), y el bundle se entera por `data-frame="page"` en su contenedor: deja de dibujar lo que el marco ya muestra y de pintar su propia hoja. La tarjeta de vista previa (`PageTip`) suma un modo GENÉRICO por atributos (`data-tip-kicker` / `-title` / `-text` / `-meta` / `-type`) con los mismos estilos, retardo y comportamiento de foco que la de página: con él, los segmentos de ejercicios de la barra de unidad —que no son páginas— por fin tienen tooltip. | Pedido del usuario del 2026-09-06: «le faltan tooltips a los pasos de los ejercicios en la barra de estado en cada unidad» y «la página de ejercicios debe estar estandarizada en base a la página de la wiki normal utilizando los mismos componentes… de forma que toda la página sea modular para futuras wikis». Una vista de herramienta que es el tramo final de una unidad tiene que leerse como una página más de esa unidad, y la única manera de que eso siga siendo cierto es que las dos dibujen el MISMO componente. | Bajo: el marco salió del lector sin cambiarle el DOM (mismos `data-seg`, `aria-label` y textos, que es lo que mira la suite E2E), y una vista sin `frame` o con un argumento que no resuelve se dibuja exactamente como antes. |
| N0-65 | **El grano de Claustro es UNA capa global encima del contenido.** La capa `.grain` de `AppRoot` (ruido SVG, `mix-blend-mode: screen`, opacidad 22 %) pasa a tener `z-index` máximo solo en Claustro, así que texturiza cada píxel por igual: superficies, rótulos pegajosos, filas activas, resaltados, diálogos y tooltips en portal. Desaparecen el token `--grain-22` y sus 29 reglas por superficie (rail, índice, cabeceras, hoja, tarjetas, paleta, menús, calendario, bundles de Proba). En los temas claros la capa sigue debajo del contenido (solo textura el fondo). | Pedido del usuario del 2026-09-06 («esos contenedores en modo oscuro se ven mal»): con el grano pintado superficie por superficie, todo hijo con fondo sólido —el rótulo pegajoso «CONCEPTOS · 4» del índice, la fila activa `--surface-2`, la página activa `--primary-soft`— quedaba MÁS OSCURO que su entorno texturizado (el grano en «screen» aclara ~+0,1 de luminancia), invirtiendo el orden `--bg < --surface < --surface-2 < --primary-soft < --elevated` que declara `tokens.css`. Una sola capa conserva ese orden sin tocar un solo token ni exigir a cada componente que recuerde el grano. | Bajo: quitar la regla de `z-index` devuelve el grano al fondo; el costo de composición de una capa fija con mezcla es despreciable (se midió el desplazamiento del lector sin caídas). |
